# Website Code Review TODOs
Generated: Tuesday, May 26, 2026

## Home Page
- [x] [Priority: LOW] [Code Quality] `js/starfield.js:12` — Uses `var` for `STARFIELD_CONFIG` instead of `const`, inconsistent with the rest of the codebase which uses `const`/`let`.
- [x] [Priority: LOW] [Code Quality] `js/main.js:540` — Uses loose equality `==` (`event.toElement == null`) instead of strict `===`. All other comparisons in this file use `===`.
- [x] [Priority: LOW] [Code Quality] `js/main.js:999` — Redundant constant reassignment `const DOUGHERTY_SEQUENCE_MS = DOUGHERTY_BLUEPRINT_SEQUENCE_MS;`. The original constant could be referenced directly.
- [x] [Priority: LOW] [Code Quality] `css/schematic.css:58-60` — Duplicate comment section markers: two consecutive `/* -------------------------------------` lines before the Hero Section heading.
- [x] [Priority: LOW] [Accessibility] `index.html:42` — Navigation links in `.meta-tiny` are placed directly adjacent with no whitespace or separator between them. Screen readers may concatenate the link text into a single continuous string.

## Resume Page
- [x] [Priority: MED] [Accessibility] `pages/resume/index.html:57` — Navigation links in `.meta-tiny` are placed directly adjacent with no whitespace or separator between them (same issue as home page). Screen readers concatenate "HOME" "RESUME" "GALLERY" "BLOG" "UTILITIES" into a single string. Add `aria-separator` or invisible space spans between links.
- [x] [Priority: LOW] [Code Quality] `css/resume.css:66-69` — Commented-out CSS block (`#typeTargetName1, #typeTargetName2 { opacity: 0; }`) is dead code. Should be removed.
- [x] [Priority: LOW] [Code Quality] `pages/resume/index.html:69` — Inline style `style="white-space: nowrap;"` on `#typeTargetName2`. Should be moved to `css/resume.css` for maintainability.
- [x] [Priority: LOW] [Code Quality] `css/resume.css:635-642` — Redundant `a.skill-tag` selector. The `.skill-tag` rules (lines 622-633) already set `color: #000000` and `text-decoration: none`, making `a.skill-tag` on line 636-637 unnecessary. The hover rule on lines 639-642 also duplicates `.skill-tag:hover`.

## Photo Gallery Page
- [x] [Priority: MED] [Error Handling] `js/gallery.js:1089` — `decodeURIComponent()` in `readPhotoHash()` can throw `URIError` on malformed percent-encoding in the URL hash (e.g., `#photo=%ZZ`). This is called from `syncGalleryFromUrl()` which fires on `hashchange` events. A crafted or corrupted hash would crash the gallery with no user-visible error. Wrap in try/catch and fall back to empty string.
- [x] [Priority: LOW] [Performance] `js/gallery.js:1047` — `refreshLightboxFocusables()` calls `window.getComputedStyle(element)` on every focusable element inside the lightbox, forcing synchronous layout recalculation. This function is called on every lightbox open, entry navigation, and info panel toggle. Consider caching the result or using a less expensive visibility check.
- [x] [Priority: LOW] [Code Quality] `js/gallery.js:375` — `entry.title` is set to `displayTitle` but never read anywhere in the codebase. Only `entry.displayTitle` is referenced (lines 591, 634, 667, 721, 726, 732, 806, 815). The `title` property is dead assignment and can be removed.

## Utilities Home Page
- [x] [Priority: MED] [Accessibility] `pages/utilities/index.html:26` — `<canvas id="starfield">` is a decorative background element but lacks `aria-hidden="true"`. Screen readers may announce it as an unlabeled image. Add `aria-hidden="true"` to suppress accessibility tree exposure.
- [x] [Priority: MED] [Accessibility] `pages/utilities/index.html:33` — `<button class="nav-back-btn">` is missing `type="button"`. Although it is not inside a `<form>`, omitting `type` defaults to `submit` per the HTML spec. If this button were ever moved inside a form, it would unexpectedly submit it. Add `type="button"` for safety.
- [x] [Priority: LOW] [Code Quality] `js/utilities-shell.js:88` — `getHashTarget()` reads `window.location.hash` which returns URL-encoded values (e.g., `#image%2Dtransform`). The utilities use hyphens which are not encoded, but if a user manually types or bookmarks a URL-encoded hash, navigation will silently fail because `VALID_UTILITIES.has(hash)` won't match. Consider `decodeURIComponent` with a try/catch fallback.
- [x] [Priority: LOW] [Code Quality] `css/utilities.css:1506` — `.audio-component-slider` sets `outline: none` which removes the default focus ring. While `focus-visible` thumb styles exist (lines 1562-1568), `outline: none` on the slider itself could interfere with browser-native focus indication for the input element. Consider `outline: revert` or scoping the outline removal to `:focus` only.
- [x] [Priority: LOW] [Code Quality] `pages/utilities/index.html:394` — Hardcoded year `2026` in the footer copyright span. The `year.js` script likely updates this dynamically, but if that script fails to load, visitors will see a stale year. Consider adding a noscript fallback or a more resilient update mechanism.
- [x] [Priority: LOW] [Code Quality] `pages/utilities/index.html:283-297` — Inline `<script type="application/json">` block for VM config. While not a security risk (it's not executable), this embeds configuration in the HTML markup. Consider moving to a data attribute on the root element or a separate config file for cleaner separation of concerns.

## Fourier Utility
- [x] [Priority: HIGH] [Performance] `utilities-src/src/audioFourierCore.ts:577-589` — `buildEnergyBandReconstruction` allocates `bandSamples` as `Float32Array(resolvedBandCount * analysis.samples.length)`. For a 7M sample signal with 12 bands this is ~336 MB of heap memory. The hard limit check at line 578 catches >512 MB, but the warning threshold at 256 MB means allocations between 256-512 MB proceed with only a progress message. On mobile or low-memory devices this can trigger OOM crashes. Consider lowering the hard limit or adding a runtime memory budget check via `performance.memory` (Chrome) before allocation.
- [x] [Priority: MED] [Error Handling] `utilities-src/src/audioFourierController.ts:593-604` — `AudioContext.resume()` failure is caught and logged but processing continues. If the browser blocks audio unlock, subsequent playback calls will also fail silently until the user interacts. The code adds a progress meta hint but doesn't update the UI state to warn the user before they press Play. Consider setting a `audioContextBlocked` flag and showing a more prominent warning on the Play button.
- [x] [Priority: MED] [Performance] `utilities-src/src/audioFourierController.ts:1139-1149` — `stopPlayback()` iterates `activeBandNodes` calling `node.source.stop()` and `node.source.disconnect()` / `node.gain.disconnect()`. The `try/catch` on `stop()` is correct (AudioBufferSourceNode throws if already stopped), but `disconnect()` on already-disconnected nodes is a no-op that still walks the AudioNode graph. Consider tracking connection state or using `node.context.createGain()` with explicit disconnect tracking.
- [x] [Priority: MED] [TypeScript] `utilities-src/src/audioFourierController.ts:386` — `requireElement` uses `ctor: { new(): T } = HTMLElement as { new(): T }`. The type assertion `as { new(): T }` on `HTMLElement` is unsafe — `HTMLElement` is not a generic constructor. This works at runtime because the `instanceof ctor` check validates the result, but TypeScript's type system can't verify the assertion. Consider using `instanceof` checks directly instead of constructor parameters.
- [x] [Priority: LOW] [Performance] `utilities-src/src/audioFourierCore.ts:622-624` — `buildEnergyBandReconstruction` mixes all bands into `mixedSamples` with a per-sample loop (`mixedSamples[sampleIndex] += band[sampleIndex]`). For 7M samples this is an O(n) operation per band, called inside the band reconstruction loop. Since `mixedSamples` is later corrected by adding residuals (lines 633-636), the intermediate mix is only used for residual computation. The loop could be replaced with a single-pass residual calculation after all bands are reconstructed.
- [x] [Priority: LOW] [Code Quality] `utilities-src/src/audioFourierController.ts:1000` — `tryAutoPlayAfterGeneration` is called with `void` (line 816), meaning any rejection is intentionally unhandled. This is correct for autoplay (browsers block it), but the function name suggests it's a "try" operation. Consider renaming to `attemptAutoPlay` or adding a comment explaining why the promise is intentionally voided.
- [x] [Priority: LOW] [Code Quality] `utilities-src/src/audioFourierWaveRenderer.ts:608-627` — `shouldSkipWebGlRenderer()` creates a probe canvas, gets a WebGL context, then checks for software renderers. The probe canvas is never removed from the DOM but also never appended, so it's garbage-collected. However, the WebGL context created on it is never explicitly lost/deleted. Consider calling `gl.makeContextLost()` or ensuring the canvas is eligible for GC by nullifying references.
- [x] [Priority: LOW] [Code Quality] `utilities-src/src/audioFourierController.ts:122-199` — Class has 37 private fields, making the constructor and `destroy()` method complex. The `reducedMotionChangeHandler` is defined as a class field arrow function (line 191-197) capturing `this`, which prevents garbage collection of the handler until the controller is destroyed. This is correct (it's cleaned up via `AbortController`), but the pattern of defining event handlers inline as class fields can be harder to reason about. Consider extracting to named private methods.
- [x] [Priority: LOW] [Code Quality] `utilities-src/src/audioFourierCore.ts:118-152` — `downsampleForDisplay` has two code paths (upsampling via interpolation when `scale < 1`, downsampling via peak detection when `scale >= 1`). The upsampling path handles the `displaySampleCount === 1` edge case specially (line 125-126), which is correct but adds complexity. Consider extracting the interpolation logic into a separate function for clarity.
- [ ] [Priority: LOW] [Code Quality] `utilities-src/src/audioFourierController.ts:83-94` — `formatSeconds` and `formatFrequency` are standalone functions that could be moved to a shared utility module. They are only used within the controller but are testable in isolation, which is good. No action needed, just noting the pattern is consistent with the rest of the codebase.
- [x] [Priority: LOW] [Browser Compatibility] `utilities-src/src/audioFourierController.ts:326-329` — `ResizeObserver` fallback uses `window.addEventListener('resize')` which only fires on window resize, not when the container element changes size (e.g., tab switching, sidebar collapse). The ResizeObserver path correctly observes the canvas elements. The fallback is a known limitation but could cause stale canvas dimensions on some browsers.
- [x] [Priority: LOW] [Test Coverage] `utilities-src/tests/audioFourierController.test.ts:1-19` — Only tests two exported utility functions (`encodeAudioFourierBandGainsCacheKey`, `shouldDebugAudioFourierWarnings`). The `AudioFourierController` class itself (1444 lines) has no tests covering state transitions, worker message handling, playback lifecycle, or canvas rendering. Consider adding integration tests for the controller's public API.
- [x] [Priority: LOW] [Test Coverage] `utilities-src/tests/audioFourierWorker.test.ts:1-49` — Tests only validate TypeScript type shapes of worker message types. No tests exercise the actual worker logic (message parsing, request queuing, cancellation, error handling). Consider adding tests that mock `self.postMessage` and verify worker behavior.

## Local Assistant Utility
- [x] [Priority: HIGH] [Performance] `js/local-llm-chat.js:1082,1100` — `renderSafeText(message.content)` is called on the full message content for every streaming token update. As the assistant response grows, each token triggers a full markdown-to-HTML reparse of the entire accumulated text. For a 500-token response this means 500 progressively heavier parses. Consider incremental rendering: append only the delta between the previous and new content, or cache the rendered HTML and append the rendered increment.
- [x] [Priority: HIGH] [Memory] `js/local-llm-chat.js:89,90` — `_messageElements` and `_renderedMessageContent` Maps accumulate entries for every message ever rendered. When `trimHistory()` (line 997) drops older messages from `this.messages`, the Maps are not pruned. DOM removal in `renderMessages()` (line 1043-1047) removes the elements from the tree but the Map references keep them alive, preventing GC. Add Map cleanup in `trimHistory()` or `renderMessages()` for pruned messages.
- [x] [Priority: MED] [Error Handling] `js/local-llm-chat.js:302` — Empty catch block `catch {` in `startChat()` has no error binding variable. While it correctly falls through to `showLoadFailure()`, the actual error object is discarded. The diagnostic detail shown to the user will lack the browser's error message, making troubleshooting harder. Bind the error and pass it to `showLoadFailure`.
- [x] [Priority: MED] [Performance] `js/local-llm-chat.js:1011-1049` — `renderMessages()` iterates all messages and compares `this._renderedMessageContent.get(message) !== message.content` for each (line 1031). For long conversations with many messages, this full re-diff on every call is expensive. The method is called from `sendMessage()`, `ensureAssistantDraft()`, `finishAssistantMessage()`, `finishInterruptedGeneration()`, `resetChat()`, `clearModelCache()`, and `endModelSession()`. Consider a more targeted update path that avoids full list re-renders.
- [x] [Priority: LOW] [Code Quality] `js/local-llm-chat.js:1394-1519` — `LocalLlmMockWorker` is a test utility class defined in the production source file. It adds ~120 lines to the production bundle. Consider moving it to a separate test fixture file imported only in test contexts, or guarding it behind the `window.__OD_LOCAL_LLM_TEST_MODE__` check in a way that allows tree-shaking.
- [x] [Priority: LOW] [Browser Compatibility] `css/local-llm-chat.css:83-84,88-89,149-151,157-159,166-168,288,296,318,437-441,484,512,552,564,570,576,620-622` — Extensive use of `color-mix(in srgb, ...)` throughout. The file correctly provides rgba() fallback declarations before each color-mix override (e.g., line 83 fallback, line 84 color-mix), which is good progressive enhancement. No action needed, noting for awareness. `color-mix()` is not supported in Safari < 16.1 or Firefox < 116.
- [x] [Priority: LOW] [Code Quality] `js/local-llm-chat.js:1548-1553` — `renderSafeInlineText()` uses regex-based markdown parsing for the loading copy animation. The `**bold**` and `*italic*` regex patterns could produce incorrect results with nested or adjacent emphasis markers (e.g., `***text***` would match as `*<strong>text</strong>*`). Since the input is controlled static strings, this is low risk, but a note for future maintenance.
- [x] [Priority: LOW] [Code Quality] `js/local-llm-worker.js:638-649` — `suppressConsoleNoise()` replaces `console.debug` and `console.info` with no-op functions during model loading. While the restore is in a `finally` block (line 134), this could mask legitimate errors from the transformers library during initialization. Consider logging to a buffer and optionally surfacing them on load failure rather than discarding entirely.
- [x] [Priority: LOW] [Code Quality] `utilities-src/src/localLlmState.ts:88` — Type assertion `as Array<{ role: 'user' | 'assistant'; content: string }>` after filtering out system messages. The assertion is safe because the preceding filter removes `'system'` role, but TypeScript cannot verify this statically. Consider using a type guard function instead of the assertion for stricter type safety.

## Virtual Machine Utility
- [ ] [Priority: MED] [Performance] `utilities-src/src/retroVmController.ts:456` — `sendAbsolutePosition()` calls `this.root.getBoundingClientRect()` on every mouse move event, triggering synchronous layout recalculation. The bounding rect only changes on resize, which is already tracked by `resizeHandler` and `ResizeObserver`. Cache the rect and invalidate it on resize to avoid forced reflows during fast mouse movement.
- [ ] [Priority: MED] [Code Quality] `utilities-src/src/retroVmController.ts:144-146` — `showConfirmModal()` has a dead conditional block. The condition `typeof document === 'undefined' || typeof window.confirm === 'function'` is always true in browser contexts, the body does nothing, and the comment suggests an unimplemented `window.confirm` fallback. If document is actually undefined, the code below would crash at `document.createElement` anyway. Remove this misleading dead code.
- [ ] [Priority: LOW] [Memory] `utilities-src/src/retroVmController.ts:215-237` — When the confirm modal is dismissed via button click (Cancel or Paste), the `handleKey` keyboard listener remains registered on `document`. Only `handleDismiss()` cleans up listeners, but the button click handlers bypass it. The overlay click listener is harmless since `overlay.remove()` detaches it, but the document keydown listener persists until Escape is pressed or the page unloads. Add listener cleanup to the button click handlers or extract `handleDismiss` into the button handlers.
- [ ] [Priority: LOW] [TypeScript] `utilities-src/src/retroVmController.ts:773` — `requireElement` uses `ctor: { new(): T } = HTMLElement as { new(): T }`. The type assertion `as { new(): T }` on `HTMLElement` is unsafe — same pattern already flagged in `audioFourierController.ts:386`. Consider using direct `instanceof` checks instead of constructor parameters.
- [ ] [Priority: LOW] [TypeScript] `utilities-src/tests/retroVmController.test.ts:136,140` — Uses `(window as any).__OD_RETRO_VM_TEST_MODE__` type assertion. The controller module already declares `__OD_RETRO_VM_TEST_MODE__` on the `Window` interface (lines 33-37), so the test could use a more specific cast like `(window as { __OD_RETRO_VM_TEST_MODE__?: boolean })` to avoid `any`.
- [ ] [Priority: LOW] [Test Quality] `utilities-src/tests/retroVmController.test.ts:175,190,217,222,247,283,288,301,307,313` — Tests rely on `await new Promise((resolve) => setTimeout(resolve, 300))` for async state transitions. These are fragile timing-based waits that could fail under slow CI or heavy load. Consider using `vi.waitFor()` or polling `dom.root.dataset.vmState` until the expected value appears.
- [ ] [Priority: LOW] [Code Quality] `utilities-src/src/retroVmController.ts:445-492` — `sendAbsolutePosition()` and `sendAbsolutePositionFromPoint()` share nearly identical logic (viewport calculation, rect check, clamping, bus send). Consider extracting the shared coordinate computation into a single private method to reduce duplication.

## Stress Test Utility
- [ ] [Priority: MED] [Code Quality] `utilities-src/src/stressTestController.ts:1463-1480` — `replaceCanvasElement()` duplicates canvas replacement logic already present inline in `startCpuVisuals()` (lines 1128-1143). Both methods create a new canvas, copy `id`, `aria-label`, `dataset.stressIdle`, and `style.cssText`, replace in DOM, rebind the resize observer, and set pixel dimensions from `getBoundingClientRect()`. The DRY violation means any future fix (e.g., copying additional attributes) must be applied in two places. Extract the shared logic into `replaceCanvasElement()` and call it from `startCpuVisuals()`.
- [ ] [Priority: LOW] [Performance] `utilities-src/src/stressTestController.ts:650` — `getWebGlContext()` sets `preserveDrawingBuffer: true` in the WebGL context options. For a stress test that draws every frame via `requestAnimationFrame`, preserving the drawing buffer between frames is unnecessary and wastes GPU memory bandwidth. The default `false` would be sufficient and more performant.
- [ ] [Priority: LOW] [TypeScript] `utilities-src/src/stressTestController.ts:1509` — `requireElement<T>(id, constructor: new () => T)` accepts a constructor parameter and uses `instanceof constructor` for runtime validation. Same unsafe constructor pattern already flagged in `audioFourierController.ts:386` and `retroVmController.ts:773`. Consider using direct `instanceof` checks (e.g., `element instanceof HTMLButtonElement`) instead of passing constructor functions, which avoids the generic constructor type assertion entirely.
- [ ] [Priority: LOW] [Code Quality] `pages/utilities/assets/stressTestController.js` — Built output is a Vite-minified bundle (121 lines, ~31 KB). Logic, constants, and shader code are consistent with the TypeScript source. No significant divergence found between source and build output.
