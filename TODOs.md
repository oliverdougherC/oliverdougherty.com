# Image Transform Utility -- Code Review TODOs

**Files under review:** `utilities-src/src/`, `scripts/generate-built-in-transform-cache.mjs`, `utilities-src/tests/`

---

## High Severity

- [x] **PERF-01** `transformCore.ts:196-210` -- ~~Replace `Map<number, number[]>` + `push()` in `buildBucketMap` with pre-allocated typed arrays for large images. On 512x512 images (262K pixels), the Map overhead + dynamic resizing adds 50-100ms.~~ **Done.** Replaced the separate source bucket map with a one-pass typed source donor index using bucket/source and bucket/group index arrays. Verified with `npm run utilities:typecheck`, `npm run utilities:test` (137 tests), and image-transform perf output from `npm run utilities:perf` (`rank=12.1ms/12.8ms/32.2ms` for representative, white-heavy, and high-detail cases).

## Medium Severity

- [x] **SEC-02** `main.ts:243-265` -- ~~Add MIME type validation in `handleFileSelection`. Reject files where `!file.type.startsWith('image/')` before processing.~~ **Done.** Added MIME type guard that shows progress error for non-image files. All 133 tests pass.
- [ ] **BUG-02** `parallelMatcher.ts:161-166` -- Fix racy progress tracking in `runRankingWorker`. `completedShortlists` increments inside `Promise.all` + `map`, so cumulative counts may not be monotonically increasing as callers might expect.
- [ ] **BUG-06** `main.ts:696-776` -- NOTE: Partially fixed. `runOnMainThread` now calls `sourceBitmap.close()` / `targetBitmap.close()` (lines 784-785) for the main thread path. However, on the `OffscreenCanvas` path (line 739-750), `ImageBitmap`s are transferred to the worker and never closed—which is correct since the worker owns them post-transfer.
- [x] **PERF-02** `transformCore.ts:218-227` -- ~~Merge two passes over source pixels (`buildBucketMap` + `buildGroupedDonorState`) into a single pass to eliminate redundant Map construction.~~ **Done.** `buildSourceDonorIndex` now classifies each source pixel once, builds exact-RGB donor groups, and emits the typed lookup structures used by both candidate collection and grouped assignment. Verified with `npm run utilities:typecheck`, `npm run utilities:test` (137 tests), and image-transform perf output from `npm run utilities:perf`; the full perf command still exits nonzero on the unrelated `audio-built-in-song-tall-dpr` RAF sample budget.
- [ ] **PERF-03** `transformIntelligence.ts:29-87` -- Optimize `computeLocalContrast` inner loop by unrolling neighbor checks and handling boundary conditions per-row/per-column instead of 4 `if` guards per pixel.
- [ ] **PERF-04** `transformCore.ts:337-399` -- Fix duplicate callback invocations in `forEachShellBucket` when `radius > 0`. Same key is visited multiple times, wasting `context.buckets.get(key)` calls.
- [ ] **PERF-05** `transformRenderPlan.ts:40-41` -- Verify the `82_000` normalization denominator. Max weighted RGB distance for pure red is ~32K, meaning `distanceNormalized` is artificially compressed and tint blending is less responsive to color deltas.
- [ ] **CQ-01** `transformCore.ts:155-172` -- Deduplicate `weightedDistance`. An identical implementation exists in `transformIntelligence.ts:107-121` (`weightedRgbDistance`) and inline in worker code. Consolidate to one shared function. **Done.** Removed local `weightedDistance` and `weightedDistanceFromChannels` from `transformCore.ts`, consolidated CIE76 distance logic into `transformIntelligence.ts` with `weightedRgbDistanceFromChannels` as the canonical function. `weightedRgbDistance` now delegates to the channel-based version. All 136 tests pass.
- [ ] **CQ-02** `transformCore.ts:175-192` -- Make `weightedDistanceFromChannels` the canonical version and have `weightedDistance` delegate to it, eliminating code duplication. **Done.** Resolved as part of CQ-01. `weightedRgbDistanceFromChannels` in `transformIntelligence.ts` is now the canonical implementation; `weightedRgbDistance` delegates to it.
- [ ] **CQ-03** `transformCore.ts:44-83` -- NOTE: Line range corrected (lines 34-42 are `TransformHooks`). Decompose `MatchingSearchContext` (39+ properties) into logical sub-structures (`BucketState`, `GroupState`, `TargetState`) to follow interface segregation principle.
- [ ] **CQ-04** `transformCore.ts:294-321` -- NOTE: `sortDonorsByUsefulness` is at lines 303-321 and comparator at 294-301. Document the hybrid sort. Name the `32` threshold as a constant and document the in-place mutation contract.
- [ ] **TS-01** `main.ts:184-186` -- Safeguard `selectedPreset` cast. Validate `this.presetSelect.value` against valid `TransformPresetId` values rather than blind casting.
- [ ] **TS-02** `main.ts:188-194` -- Add runtime type assertion in `requireElement` to verify the DOM element is actually the expected tag type, not just a truthy element with the same ID.
- [ ] **TEST-01** `tests/transformCore.test.ts` -- NOTE: One test exists for the fallback path at lines 309-329 ("continues fallback search"). Add more coverage for `findBestAvailableSourceIndex` edge cases.
- [ ] **TEST-03** `tests/` -- NOTE: `workerRuntime.ts` IS tested in `workerRuntime.test.ts` (3 tests covering request handling, error propagation, and prepared-bitmap deflation). Still missing: cancellation tests. Add tests for `transform.worker.ts` request handling, cancellation, and prepared-bitmap inflation/deflation.
- [ ] **TEST-04** `tests/` -- Add unit tests for `main.ts` (`UtilitiesApp`). The UI controller has zero test coverage.
- [ ] **API-01** `transformCore.ts:120-134` -- Export a `ResolvedDimensions` interface for `resolveOutputDimensions` return value instead of relying on inferred literal types.
- [ ] **API-02** `transformCache.ts:61-78` -- Replace `::` delimiter in `buildBuiltInTransformCacheKey` with a more robust key scheme. If a URL contains `::`, the key becomes ambiguous. **Done.** Replaced `::` with Unit Separator character (`\u001f`). Updated test assertion in `transformCache.test.ts`. All 136 tests pass.
- [ ] **API-03** `types.ts:3-4` -- Derive `TransformPresetId` type from `TRANSFORM_PRESETS` record keys to keep the type and runtime data in sync, preventing stale type definitions when presets are added. **Done.** Made `TRANSFORM_PRESETS` use `as const satisfies Record<TransformPresetId, TransformPreset>` for compile-time validation of preset keys against the `TransformPresetId` union type. Added `VALID_PRESET_IDS` export for runtime validation. All 136 tests pass.
- [ ] **CFG-01** `presets.ts:3-28` -- Extract hardcoded preset values (`maxDimension`, `quantizationBits`, `animationDurationMs`, `animationParticleBudget`) into configurable constants or a settings object.
- [ ] **CFG-02** `parallelMatcher.ts:9-13` -- Document `PARALLEL_MATCH_MIN_PIXELS` (160K) behavior. At 512x512 = 262K pixels it activates, but the threshold and the permanent `false` experimental flag should be documented.
- [ ] **COMP-01** `main.ts:243-266` -- Add file size limit on uploaded images. A 100MB PNG would be decoded and scaled, consuming gigabytes of memory during pixel extraction. Enforce a max (e.g., 20MB) with user-facing rejection message.
- [ ] **COMP-02** `main.ts:1176-1211` -- NOTE: Line range now covers `playAnimation` at lines 1169-1211. Animation uses `requestAnimationFrame` which is capped by display refresh rate. On ProMotion displays would render at up to 120fps. Consider a frame limiter for predictable behavior and reduced battery drain.
- [ ] **COMP-06** `parallelMatcher.ts:60-67` -- `EXPERIMENTAL_PARALLEL_MATCHER_ENABLED` is permanently `false`, making `parallelMatcher.ts`, `matchingWorkerLogic.ts`, and `matching.worker.ts` dead code in production. Either enable, remove, or tree-shake.

## Low Severity

- [ ] **SEC-03** `main.ts:278-279` -- NOTE: Line range now `clearActiveDemo` (lines 278-280). No file-size guard exists in drag-and-drop handler (`bindDropzone`, line 216-241), which calls `handleFileSelection`. A very large file (2GB BMP) will stall the main thread during pixel extraction.
- [ ] **SEC-04** `main.ts:216-241` -- NOTE: `bindDropzone` uses `dragEvent.dataTransfer?.files?.[0]` which only returns actual File objects. URLs and directories wouldn't appear in `dataTransfer.files`. Partially mitigated by the browser API itself, but consider explicit filtering for robustness.
- [ ] **SEC-05** `generate-built-in-transform-cache.mjs:1` -- NOTE: `url` values come from hardcoded `DEMOS` record. Path traversal is not a risk with hardcoded values. Add `path.resolve` + containment checks as a defense-in-depth measure. Minor/info-level concern.
- [ ] **BUG-03** `transformRenderPlan.ts:24-25` -- Fix redundant analysis computation in `main.ts:1005-1020`. `buildTransformRenderPlan` recomputes `analysis` when called directly, rather than reusing the existing `transform.analysis`.
- [ ] **BUG-04** `transformAnimation.ts:136-144` -- NOTE: `clamp` on `currentX`/`currentY` handles boundary correctly. Multiple source pixels landing on same target position is handled by `drawPriority` logic with `positionPriorityScratch`. This is designed behavior, not a bug. The concern is valid but not actionable.
- [ ] **BUG-07** `generate-built-in-transform-cache.mjs:73-85` -- NOTE: Lines 73-85 now cover `compileModule` with `matchAll` on import regex (line 74). The regex `/^(?:import|export)\s+...` does match imports inside template literals at the top of lines. Minor concern for robustness.
- [ ] **PERF-06** `transformAnimation.ts:122-123` -- Optimize per-frame `destination.fill(0)` and `positionPriorityScratch.fill(-1)` on 262K elements at 60fps (~31M writes/sec on large images).
- [ ] **PERF-07** `main.ts:1134-1142` -- NOTE: Line 1093 now `context.putImageData(new ImageData(...))`. The `renderAnimationFrame` is at lines 1125-1148. The `new Uint8ClampedArray(framePixels)` at line 1140 only happens in the `else` path. Avoid unnecessary allocation on every frame.
- [ ] **PERF-08** `parallelMatcher.ts:86-127` -- NOTE: Line range corrected. `runRankingWorker` creates workers via `createWorker()` on line 92 and terminates on line 100/110/123. Reuse workers across transforms instead of creating/destroying per-request.
- [ ] **CQ-05** `transformCore.ts:294-301` -- Rename `compareDonorsByUsefulness` to `compareToDonorsByUsefulness` or `compareForDonorSort` to convey it's a sort comparator.
- [ ] **CQ-06** `main.ts:35-37` / `workerRuntime.ts:13-15` -- Deduplicate `asArrayBuffer`. Share between both files.
- [x] **CQ-07** `main.ts` / `transformAnimation.ts` / `transformRenderPlan.ts` / `transformIntelligence.ts` / `math.ts` -- Consolidate 5 copies of `clamp` into one shared utility import. **Done.** Replaced local `clamp` in all 4 files with `import { clamp } from './math'`. All 133 tests pass.
- [x] **CQ-08** `transformCore.ts:113-114` -- ~~Remove dead constant `INITIAL_SHORTLIST_SIZE`. It's defined but never referenced.~~ **Done.**
- [ ] **CQ-09** `main.ts:1050-1052` -- NOTE: Lines 1050-1052 now the reduced-motion `setProgress` call followed by blank lines. Remove trailing blank line. Inconsistent formatting.
- [ ] **CQ-10** `main.ts:1055-1061` -- NOTE: Lines now `setProgress` + `playAnimation()` call block. Trailing blank lines present but line numbers shifted. Remove trailing blank line block.
- [x] **CQ-11** `transformCore.ts:410-412` -- ~~NOTE: Magic numbers now at lines 410-412 in `scoreCandidateDistance` (`50_000`, `34_000`, `14_000`). Extract into named constants.~~ **Done.**
- [ ] **CQ-13** `transformAnimation.ts:140-144` -- Document magic constants in `drawPriority` formula (`4`, `1`, `0.01`, `1_000_000`).
- [ ] **CQ-14** `transformRenderPlan.ts:44-57` -- Document or extract blending coefficient magic numbers (`0.48`, `0.78`, `0.72`, `0.12`, `0.45`, `0.42`).
- [ ] **TS-03** `workerRuntime.ts:13-15` -- Handle `SharedArrayBuffer` in `asArrayBuffer` instead of blind cast from `ArrayBufferLike`.
- [ ] **TS-04** `main.ts:35-37` -- Same unsafe `asArrayBuffer` cast as TS-03. (See CQ-06 for dedup.)
- [ ] **TS-05** `transformCache.ts:6-8` -- Handle `SharedArrayBuffer` in `cloneArrayBuffer`. `SharedArrayBuffer` lacks `slice`, so the cast + `slice(0)` will throw at runtime.
- [ ] **TS-06** `transformCache.ts:34` -- Handle `SharedArrayBuffer` in `arrayBufferToBase64` to avoid blind `ArrayBufferLike` cast.
- [ ] **BUG-06** `main.ts:732` -- NOTE: Now at `await this.runOnMainThread(...)`. The bitmap closing concern was addressed in `runOnMainThread` at lines 784-785. For the worker path, the bitmap is transferred (not closed), which is correct.
- [ ] **TEST-05** `tests/transformCore.test.ts:279-283` -- Add edge case tests for `validateMatchingInputs`: `quantizationBits < 4`, `quantizationBits > 8`, mismatched array lengths. **Done.** Added 3 tests: quantization bits below 4, above 8, and mismatched source/target pixel counts. Test count increased from 133 to 136.
- [ ] **TEST-06** `tests/transformAnimation.test.ts` -- Add tests for phase values outside [0, 1] (clamp behavior) and zero-sized images.
- [ ] **TEST-07** `tests/transformCache.test.ts:20-69` -- NOTE: `sliceArrayBufferView` alignment handling is tested in `workerRuntime.test.ts` (line 61-104), but not directly in transformCache tests. Test with non-zero `byteOffset` buffers for completeness.
- [ ] **API-04** `parallelMatcher.ts:30-33` -- NOTE: Line range corrected. `ParallelMatchProgressHooks` extends `TransformHooks` but callers don't use extended hooks directly—they're only used in `matchPackedPixelsInParallel` which takes `ParallelMatchRequest`. Type mismatch still exists.
- [ ] **CFG-03** `main.ts:44` -- Replace static `matchMedia` snapshot with `addEventListener('change')` so `reducedMotion` updates when user changes system preference at runtime. **Done.** Replaced `readonly reducedMotion` with mutable private field and added `MediaQueryList` listener in `init()`. All 136 tests pass.
- [ ] **COMP-03** `main.ts:1281-1287` -- NOTE: Line range corrected. Explicitly set `mode: 'same-origin'` in `fetch(selection.url)` for demo assets. Currently relies on implicit same-origin behavior.
- [ ] **COMP-07** `main.ts:1180-1210` -- Add `visibilitychange` listener to pause animation when tab is backgrounded, preventing animation jumps on tab return.

## Info Severity

- [ ] **BUG-05** `transformCore.ts:929` -- Add comment explaining the dual-path `maybeReportProgress` logic (`completed === total` or modulo), documenting that the final progress is always reported.
- [ ] **BUG-08** `transformCache.ts:39-44` -- Document that `0x8000` (32KB) chunk size in `arrayBufferToBase64` stays below V8's ~65K argument limit for `String.fromCharCode(...)`.
- [x] **CQ-12** `transformRenderPlan.ts:41` -- ~~Replace magic number `82_000` with named constant `MAX_WEIGHTED_RGB_DISTANCE` and add comment explaining its origin.~~ **Done.**
- [ ] **TS-07** `workerRuntime.ts:17-19` -- Document the `ArrayBufferLike` to `ArrayBuffer` cast in `sliceArrayBufferView` to acknowledge `SharedArrayBuffer` distinction is intentionally lost.
- [ ] **TS-08** `types.ts:74-81` -- Add comment noting that `PreparedImageTransfer.pixels` is intentionally `ArrayBuffer` (not `ArrayBufferLike`) since workers always use transferable buffers.
- [ ] **TEST-08** `tests/transformRenderPlan.test.ts:53` -- NOTE: `expect(Math.min(...)).toBeLessThan(0.08)` verifies tint range exists but not which pixels have low tint. Strengthen assertion.
- [ ] **TEST-09** `tests/parallelMatcher.test.ts:71-131` -- Add tests for real async worker timing, race conditions between workers, and `Promise.all` settlement under failure.
- [ ] **TEST-10** `tests/uiState.test.ts` -- Add coverage for `resolvePlaybackButtonLabel` when `isProcessing` or `isAnimating` returns `'Play'`. Only 3 test cases total.
- [ ] **TEST-11** No integration tests exist for the full pipeline (`selectionToBitmap` -> `generateTransform` -> worker -> `applyTransformSuccess` -> animation). Add a proper integration test suite beyond the browser-based `utilities-check.js`.
- [ ] **API-05** `transformCore.ts:34-42` -- Rename `isCancelled` in `TransformHooks` to `onCheckCancelled` or change to a boolean to be consistent with the `on*` callback naming pattern.
- [ ] **API-06** `workerRuntime.ts:56-67` -- Make the fire-and-forget error handling in `handleWorkerRequest` more explicit with type annotations or a wrapper.
- [ ] **DEP-01** `generate-built-in-transform-cache.mjs:6-7` -- Verify `sharp` is in `devDependencies` and CI steps include native build tool installation for platform-specific binary resolution.
- [ ] **DEP-02** `generate-built-in-transform-cache.mjs:87-95` -- NOTE: `ts.transpileModule` call now at lines 87-95. Skips type checking and ignores `tsconfig.json`. Consider running `tsc --noEmit` separately or using the full compiler API to catch type errors at cache build time.
- [ ] **DEP-03** `transformCache.ts:35-38` -- Document the `typeof Buffer !== 'undefined'` runtime check for Node.js vs browser environment detection. Behavior depends on bundler configuration.
- [ ] **CFG-04** `generate-built-in-transform-cache.mjs:22-33` -- Document `TRANSFORM_CACHE_PRESETS` and `TRANSFORM_CACHE_DEMOS` environment variables.
- [ ] **CFG-05** `main.ts:81-82` -- Cache size is bounded (3 demos x 3 presets = 9 max), so the no-LRU concern is minor. Add a comment confirming the bounded size to prevent future confusion.
- [ ] **COMP-05** `main.ts:322-338` -- NOTE: Line range now `syncButtons` method. Unicode symbols (`↻`, `⏸`, `▶`) in button text have `aria-label` set but no visible accessible text. Consider adding `sr-only` text as a fallback.

---

# Audio Fourier Utility -- Code Review TODOs

**Files under review:** `utilities-src/src/audioFourierCore.ts`, `utilities-src/src/audioFourierController.ts`, `utilities-src/src/audioFourier.worker.ts`, `utilities-src/src/audioFourierUiState.ts`, `utilities-src/src/audioFourierWorkerTypes.ts`, `utilities-src/src/fft.ts`, `utilities-src/src/audioPresets.ts`, `utilities-src/src/audioSignal.ts`, `utilities-src/src/math.ts`, `utilities-src/tests/` (audioFourier*.test.ts)

---

## High Severity

- [x] **BUG-FA-01** `audioFourierCore.ts:326-348` -- ~~`buildWindowedFourierAnalysis` calls `frameInput.fill(0)` every frame iteration, though only the last frame actually needs zero-padding beyond `samples.length`. Move the fill outside the loop (once) and only write the overlapping portion per frame, then zero-pad only the tail of the last frame. For a 3.8M sample signal this saves ~300K `fill` calls.~~

  **Resolved:** Removed `frameInput.fill(0)` from inside the loop — the inner loop writes every position of `frameInput` via the ternary, so the fill was redundant. Added 4 new edge-case tests. All 129 tests pass.
- [ ] **PERF-FA-01** `audioFourierCore.ts:350-356` -- `order.sort()` on a plain JS number array for potentially millions of coefficients. For a 5-minute track at 8kHz, `coefficientCount` can exceed 1.4M. The sort callback invokes `coefficientFrequency` for every tie comparison. Precompute frequencies or cache the modulo result to avoid redundant arithmetic in the O(N log N) comparator.
- [ ] **MEM-FA-01** `audioFourierCore.ts:530-586` -- `buildEnergyBandReconstruction` allocates `bandSamples` of size `bandCount x sampleCount` (~336 MB for 12 bands of 7M samples), plus `reconstructionScratch.spectraReal/spectraImag` (~61 MB for 7489 frames x 1024 x 2). Peak memory can exceed 500 MB, risking out-of-memory on low-end devices. Consider streaming band reconstruction or releasing intermediate bands after mixing.
- [ ] **PERF-FA-02** `audioFourierCore.ts:596-614` -- `mixEnergyBands` iterates over `bandCount x sampleCount` (up to 84M at 12 bands and 7M samples) in a tight JS loop. Consider `WebAssembly` or typed-array SIMD intrinsics for the inner summation loop to achieve 5-10x throughput improvement.
- [ ] **PERF-FA-03** `fft.ts:57-77` -- `fftInto` recomputes `Math.cos` and `Math.sin` for every butterfly operation. For a 4096-point FFT this is ~16K trig calls per FFT. Precompute twiddle factors for each iteration level in the `FftWorkspace` to amortize trig cost across all FFT calls.
- [ ] **BUG-FA-02** `audioSignal.ts:92-112` -- `resampleLinear` uses linear interpolation, which introduces aliasing when downsampled significantly (e.g., 44.1kHz to 8kHz is a 5.5x reduction). Replace with a windowed sinc resampler or polyphase filter to avoid pre-alias artifacts in the proxy signal.
- [ ] **MEM-FA-02** `audioFourierCore.ts:416-426` -- `createReconstructionScratch` allocates `spectraReal`, `spectraImag` of size `frameCount x frameSize`, which for 7489 frames x 1024 each creates ~61 MB for the pair. Combined with `bandSamples` (336 MB), the worker can pressure browser memory limits. Consider a single-frame scratch buffer that reconstructs one frame at a time instead of materializing all frequencies simultaneously.
- [ ] **CORRECTNESS-FA-01** `audioFourierCore.ts:600-601` -- `mixEnergyBands` default parameter `destination = new Float32Array(sampleCount)` creates a fresh buffer every call when omitted. The `destination.fill(0)` on line 602 clears it, but if the caller passes a pre-warmed buffer, the zero-fill is still correct. However, the function returns the destination, and the caller in `buildEnergyBandReconstruction` line 569 passes one. Ensure all call sites account for the buffer being mutated in place.

## Medium Severity

- [ ] **BUG-FA-03** `audioFourierController.ts:657-676` -- `getWorker` creates the worker lazily, but `abandonActiveComputation` (line 531) terminates it and sets `this.worker = null`. When `getWorker` is called again, new event listeners are attached. The old worker's listeners fire on a dead Worker object, but if `abandonActiveComputation` races with an in-flight message dispatch, the `handleWorkerMessage` callback could run after `this.activeRequestId` has advanced, bypassing the requestId guard at line 689 if the IDs happen to match. Add an explicit "worker version" counter.
- [ ] **BUG-FA-04** `audioFourierController.ts:992-993` -- `pausePlayback` computes `playbackElapsedSeconds` from `context.currentTime - this.playbackStartedAt`. The AudioContext clock drifts from wall clock when the tab is backgrounded, causing resume to jump to an incorrect position. Store `playbackElapsedSeconds` in the last `tickPlayback` frame instead, or reconcile with the visual clock on resume.
- [ ] **BUG-FA-05** `audioFourierController.ts:1033-1035` -- `updateLiveBandGains` calls `cancelScheduledValues` + `setTargetAtTime` on the master gain during slider movement. Rapid slider changes can cause the exponential ramp to never reach the target before the next call, resulting in audible gain lag (or staircase artifacts at the time constant). Consider using `linearRampToValueAtTime` with a short horizon for interactive gain changes.
- [ ] **CORRECTNESS-FA-02** `audioFourierController.ts:843-850` -- `resolveApproximateActiveComponents` assumes linear component distribution within each band by interpolating `(endComponentCount - previousComponentCount) * gain`. Components within a band have varying energies, and gain applies uniformly to all. The reported "active components" count is a rough approximation that doesn't correspond to perceptual energy. Document this limitation or use cumulative energy instead.
- [ ] **TS-FA-01** `audioFourier.worker.ts:242` -- `(request as any).requestId` uses an explicit `any` cast for the error message. Use a type guard or `satisfies` to narrow the `AudioFourierWorkerRequest` union instead.
- [ ] **TS-FA-02** `audioFourierController.ts:61-68` -- `asArrayBuffer` is duplicated between `audioFourierController.ts` and `audioFourier.worker.ts`. Extract to a shared utility module (e.g., `bufferUtils.ts`).
- [ ] **TS-FA-03** `audioFourierController.ts:950` -- `gain.gain.value = this.activeResult?.bandGains[index] ?? 0`. The optional chaining on `this.activeResult` is dead code -- line 928 already guards `!this.activeResult`. Remove the defensive `??` and use direct access.
- [ ] **CORRECTNESS-FA-03** `audioFourierCore.ts:85-98` -- `createHannWindow` with `size === 1` returns `[1]`, not `[0]` as the Hann formula would suggest (it has a division-by-zero at N=1). The special case is reasonable but should be documented since Hann(1) is mathematically undefined and this choice affects frame edges for pathological input lengths.
- [ ] **CORRECTNESS-FA-04** `audioFourierCore.ts:375-391` -- `buildWindowedFourierAnalysis` clones `samples` via `new Float32Array(samples)` at line 376, but also passes `samples` by reference to `downsampleForDisplay` at line 390. Any caller that mutates the original `samples` buffer after the analysis is built could corrupt `finalDisplayFrame` if it's a view (it isn't, but the intent is fragile). Document the immutability contract.
- [ ] **CFG-FA-01** `audioPresets.ts:48-85` -- All three quality presets share `energyBandCount: 12` and `sliderSteps: 100`. The `detailed` preset could benefit from more bands (e.g., 20) for finer-grained energy selection, and fewer slider steps could smooth the `fast` preset UX. Tie band count to analysis resolution.
- [ ] **CFG-FA-02** `audioPresets.ts:61-64` -- `detailed` preset has `maxProxySampleCount: 8_000_000` at `proxySampleRate: 32_000`, allowing ~250 seconds, but `maxDurationSeconds` is 480 (8 minutes). Songs up to 5 minutes will be silently truncated to ~250 seconds by `prepareAudioSignal` with no user-facing message.
- [ ] **BUG-FA-06** `audioPresets.ts:87-93` -- NOTE: The `!preset` guard at line 89 catches `undefined` and throws a descriptive error. The runtime error concern is already handled, but adding a value check against `AUDIO_FOURIER_PRESETS` keys before casting would be more defensive.
- [ ] **SEC-FA-01** `audioFourier.worker.ts:46-48` -- The worker accepts `request.source.channelBuffers` as potentially untrusted `ArrayBuffer[]`. No validation that buffer sizes are consistent with `sampleRate` and a reasonable duration bound. A pathological buffer could allocate excessive memory for the `Float32Array` view on line 47.
- [ ] **CORRECTNESS-FA-05** `audioSignal.ts:31-57` -- `downmixToMono` uses a simple arithmetic mean across all channels. For surround sound (5.1+ channels), this treats LFE and surround channels with equal weight, resulting in incorrect loudness balance. Use ITU-R BS.1770 or at least channel-dependent coefficients for the downmix.
- [ ] **CORRECTNESS-FA-06** `audioFourierCore.ts:221-248` -- NOTE: The claim about "unexpected results" for single-element ranges is overstated—the clamp at line 229 ensures `end >= start + 1`. However, if all-NaN, line 243 returns `{ min: samples[start], max: samples[start] }` which can be NaN (the `??` operator doesn't catch NaN). The TODO's concern is partially valid for NaN handling.
- [ ] **CQ-FA-01** `audioFourierCore.ts:152-163` -- Magic number cluster for slider curves and loudness compensation. `SLIDER_CURVE_EXPONENT` (1.85), slider breakpoint ratios (0.5, 0.8, 0.2, 0.6), `FULL_ENERGY_VISUAL_THRESHOLD` (0.999 in controller), and `VISUAL_ORIGINAL_CLAMP_*` (0.8, 0.85) are named but their perceptual derivations are undocumented. Add inline comments explaining the Fletcher-Munson curves or equal-loudness rationale.
- [ ] **CQ-FA-02** `audioFourierCore.ts:76-83` -- The `ReconstructionScratch` interface is private but referenced in the public `reconstructWindowedComponentRange` signature. Its memory footprint is significant (~61 MB for large analysis). Consider exposing a `ReconstructionScratchSize` estimate for callers to budget ahead.
- [ ] **COMP-FA-01** `audioFourierController.ts:678-685` -- `handleWorkerFailure` requires a full page reload. Offer a "Retry" action instead, reusing the existing `generate()` method without forcing the user to reload.
- [ ] **CQ-FA-03** `audioFourierController.ts:836-851` -- `resolveApproximateActiveComponents` iterates over band gains to estimate component count. This O(bandCount) function is called on every slider input (line 806) and every render frame (line 1375). Cache the result when band gains haven't changed.

## Low Severity

- [ ] **BUG-FA-08** `audioFourierController.ts:963-976` -- `playPlayback` only attaches `onended` to `this.activeBandNodes[0].source`. If band nodes finish at different times (due to gain ramps or numerical padding), subsequent nodes' `onended` callbacks are ignored. This is acceptable for the current design (all nodes have the same duration), but document the assumption.
- [ ] **BUG-FA-09** `audioFourierController.ts:1004-1008` -- `stopPlayback` calls `node.source.stop()` wrapped in try-catch. The comment says "harmless," but the `InvalidStateError` from stopping an already-ended source is expected and should be checked explicitly (`if (node.source.state !== 'closed')`) to avoid try-catch overhead on every stop.
- [x] **CQ-FA-04** `audioFourierUiState.ts:1` -- ~~`resolveAudioPlaybackButtonLabel` accepts `elapsedSeconds` but never reads it. The parameter is dead, likely residual from a removed code path. Either remove the parameter or implement the intended elapsed-based label logic.~~ **Done.** Removed `elapsedSeconds` from `resolveAudioPlaybackButtonLabel`, its call site in `audioFourierController.ts`, and updated 6 test cases in `audioFourierUiState.test.ts`. All 133 tests pass.
- [ ] **CQ-FA-05** `audioFourierController.ts:87-95` -- Inline constants (`INITIAL_SLIDER_VALUE`, `PLAYBACK_FADE_SECONDS`, `GAIN_RAMP_TIME_CONSTANT`, etc.) span lines 87-95 without a section header. Group by concern: playback, UI, and rendering. Move to a config object for easier experimentation.
- [ ] **CQ-FA-06** `audioFourierController.ts:1109-1124` -- `clearCanvas` uses a chain of ternary conditionals to determine the background canvas. This pattern doesn't scale if canvases are added. Use a `Map<HTMLCanvasElement, HTMLCanvasElement>` or named properties.
- [ ] **CQ-FA-07** `audioFourierController.ts:1299-1301` -- `drawWaveformEnvelope` sets both `context.shadowBlur = 0` and `context.shadowColor = 'transparent'` when glow is disabled. `shadowBlur = 0` alone disables shadow rendering. Remove the redundant `shadowColor = 'transparent'` assignment.
- [ ] **CQ-FA-08** `audioFourierController.ts:1070` -- `drift * 0.08` in `tickPlayback` is a magic damping coefficient (equivalent to a ~12.5 frame exponential smoothing time constant). Name it `VISUAL_CLOCK_DAMPINGFACTOR` with a comment explaining the reconciliation trade-off.
- [ ] **CQ-FA-09** `audioFourierController.ts:1399-1407` -- `drawComponentFrame` iterates pixel-by-pixel across canvas width for the sine wave. A 1920px canvas generates 1920 `lineTo` calls per frame. Subsample to ~960px (every 2nd pixel) or use quadratic beziers for equivalent visual quality at half the work.
- [ ] **CQ-FA-10** `audioFourierController.ts:74-86` -- `formatSeconds` and `formatFrequency` are module-level functions only used by the controller. Make them private static methods of `AudioFourierController` or move to a shared formatting utility.
- [ ] **CQ-FA-11** `audioFourier.worker.ts:239-245` -- Unrecognized message types are handled after the cancel check. The error message includes `(request as any).requestId` which can be `undefined`. Use the worker request ID counter for the error message instead.
- [ ] **CQ-FA-12** `audioFourierCore.ts:137-140` -- `coefficientFrequency` takes 4 parameters but only uses `coefficientIndex % binCount`. The `frameSize` and `sampleRate` parameters are fixed per-analysis, so passing them per-call is overhead. Consider a factory function that returns a closure over the constants.
- [ ] **CQ-FA-13** `audioFourierCore.ts:463-466` -- The conjugate symmetry write `spectraReal[frameOffset + frameSize - bin] = real` is correct but could be clearer with a named variable. Add `const mirrorBin = frameSize - bin` with a comment referencing the real-signal Hermitian symmetry property.
- [ ] **CQ-FA-14** `audioFourierWorkerTypes.ts:23` -- The union type `AudioFourierWorkerRequest` could use a discriminated union with a shared `requestId` property extracted to a base interface, making the type more composable and the `requestId` less fragile to add.
- [ ] **TEST-FA-01** `tests/audioFourierController.test.ts:1-56` -- Only tests shared `clamp` and `assertPowerOfTwo` utilities, not controller logic. The AudioFourierController class (1438 lines) has zero coverage. Add at least DOM-mocked tests for state transitions, slider handling, and playback lifecycle.
- [ ] **TEST-FA-02** `tests/audioFourierWorker.test.ts:1-49` -- Only verifies TypeScript type shapes of request interfaces. No runtime tests for worker message routing, cancellation, queuing, error propagation, or ArrayBuffer transfers. Add jsdom/jest-worker or integration-level tests.
- [ ] **TEST-FA-03** `tests/audioFourierCore.test.ts` -- Missing tests for:
  - `mixEnergyBands` with mismatched bandSamples length
  - `reconstructWindowedComponentRange` with overlapping or invalid ranges
  - `buildWindowedFourierAnalysis` with NaN/Infinity samples
  - `resolveViewportRange` with fractional viewport seconds
  - `buildEnergyBandEnvelopes` with non-aligned sample counts
  - `resolveSampleEnvelope` on all-NaN array (should return 0/0)
- [ ] **TEST-FA-04** No tests for `audioSignal.ts` functions: `downmixToMono`, `normalizeSignal`, `resampleLinear`, `prepareAudioSignal`. These are critical path functions with known edge cases (silent audio, surround sound, short signals).
- [ ] **TEST-FA-05** No tests for `fft.ts`: `fft`, `fftInto`, `createFftWorkspace`. The FFT implementation has no test coverage. At minimum, verify forward-inverse round-trip accuracy and DC/Nyquist handling.
- [ ] **API-FA-01** `audioFourierWorkerTypes.ts:53-58` -- `timingsMs` is missing an `envelopes` phase. The worker builds envelopes after band reconstruction (line 114-128 of worker), but the elapsed time isn't measured. Add an `envelopes` timing bucket for completeness.
- [ ] **API-FA-02** `audioFourierCore.ts:35-43` -- `EnergyBandReconstruction` returns `mixedDisplayFrame` but not a corresponding `mixedSampleCount`. The `sampleCount` is separate. Consider bundling for completeness or renaming to `bandSampleCount`.
- [ ] **API-FA-03** `audioFourierCore.ts:616-618` -- `resolveEnvelopeBucketSampleCount` returns `number` but has no exported `EnvelopeBucketConfig` type. The caller in the worker (line 114) computes the same value as the envelope construction downstream, risking misalignment if parameters diverge.
- [ ] **COMP-FA-02** `audioFourierController.ts:1419-1437` -- `destroy()` is public but undocumented. Document the lifecycle contract (`constructor` -> `init()` -> ... -> `destroy()`) and whether `destroy()` can be called multiple times safely (it can't -- `requireElement` errors are thrown at construction, but subsequent `destroy()` calls would abort the already-aborted controller).
- [ ] **COMP-FA-03** `audioFourierController.ts:328-333` -- `resizeCanvases` computes DPR but doesn't clamp it to a practical maximum (e.g., 3). On a 4x display with a large element, canvas internal resolution could exceed GPU texture limits. Add `Math.min(dpr, 3)`.
- [ ] **SEC-FA-02** `audioFourierController.ts:599-617` -- `resolveAudioSource` fetches built-in presets with `fetch(preset.url)` but doesn't set `mode: 'same-origin'` or `credentials`. If the FLAC files are ever hosted on a CDN or different origin, CORS issues could break preset loading silently.
- [ ] **CQ-FA-15** `audioFourierCore.ts:714-720` -- `mixEnergyBandEnvelopes` default parameter creates a new `MixedAudioEnvelope` with `approximate: true`. This default object is created at function definition time, not call time (it's an expression, so it's created per call). However, the function reassigns `destination` on line 724 if sizes don't match. If the caller passes `undefined`, the default provides a fresh buffer, which is correct but could confuse linters expecting no mutable defaults. Make the default explicit with `destination = undefined` and handle inside the body.
- [ ] **PERF-FA-04** `audioFourierCore.ts:619-656` -- `buildSampleEnvelope` could be optimized with a Web Worker or chunked processing for large signals (7M+ samples). Each bucket requires iterating over `bucketSampleCount` samples in a JS loop, which for ~9 buckets/sec x 8kHz sample rate = 8957 iterations x 7M / 8957 = 782 bucket iterations, each with ~8957 inner iterations.

---

# Local Assistant Utility -- Code Review TODOs

**Files under review:** `js/local-llm-chat.js`, `js/local-llm-worker.js`, `js/local-llm-config.js`, `css/local-llm-chat.css`, `utilities-src/src/localLlmState.ts`, `utilities-src/tests/localLlmState.test.ts`

---

## High Severity

- [ ] **SEC-LLM-04** `local-llm-worker.js:11-37` -- The worker message handler is synchronous. A buggy main thread could flood `generate` messages, though `activeGeneration` gating discards stale requests. No queue length limit exists. Consider a pending-queue cap to prevent memory pressure from rapid-fire messages.
- [ ] **BUG-LLM-01** `local-llm-chat.js:253-266` -- NOTE: The catch block at line 256-266 IS specific: "This browser could not create the local model worker." This catches Worker **construction** failure only. CDN failures for `transformers.js` are handled inside the worker via `categorizeError()->runtime-import`, not this catch. A separate "CDN unavailable" path here is unnecessary, but the main thread's catch block for worker startup failures could be more specific.
- [ ] **BUG-LLM-02** `local-llm-worker.js:115-121` -- NOTE: Cannot happen through normal UI. `sendMessage()` sets `status = THINKING`, and the form submit guard at line 174-178 blocks a second send (triggers `interruptGeneration()` instead). The concern is theoretical only.
- [ ] **BUG-LLM-03** `local-llm-chat.js:764-768` -- `trimHistory` keeps the last 10 chat messages + up to 2 notices. The user's visible chat may show 12 messages, but only the last 10 are sent to the model. No notification is given that earlier messages were dropped from the context window. Add a system notice when history is trimmed.
- [x] **BUG-LLM-05** `local-llm-chat.js:340-350` -- ~~The `disposed` worker message handler sets `this.worker = null` and `this.modelReady = false`. However `this.assistantDraft` is not cleared, so if an assistant draft was accumulating when the worker died, it remains in `this.messages` as a partial message. The subsequent `updateStatus(WORKER_STATE.IDLE, ...)` doesn't fix the dangling draft.~~ **Done.** Added cleanup of `assistantDraft` in the `disposed` handler: filters the draft from `this.messages` and sets `this.assistantDraft = null`.
- [ ] **BUG-LLM-06** `local-llm-worker.js:39-65` -- `loadModel` catches errors from `loadModelInternal`, posts a structured error via `setState`, then re-throws. The outer catch (lines 62-65) swallows the re-thrown error. This prevents unhandled rejection warnings but also suppresses `console.error` traces. Add `console.error(error)` for debugging.
- [ ] **PERF-LLM-01** `local-llm-chat.js:770-794` -- `renderMessages` rebuilds ALL message DOM nodes on every call, discarding and recreating the entire message list. For a 10-message conversation with long responses, this is unnecessary work. Consider a keyed virtual-dom approach, or at minimum reuse existing `<article>` elements for messages whose content hasn't changed.
- [ ] **PERF-LLM-02** `local-llm-chat.js:797-803` -- `updateAssistantElement` reconstructs the full `innerHTML` of the assistant message content on every token. For a 100-token streaming response, this means 100 full `renderSafeText` + `innerHTML` cycles. Use incremental text appending (text node + `<span>` tokens) to avoid full re-parsing.
- [ ] **PERF-LLM-03** `local-llm-chat.js:549-585` -- `renderStatePanel` runs on every status update, loading sequence step (every 2.5s), and after `renderMessages`. The innerHTML assignment on `this.loadCopy` uses a fade transition (300ms timeout). On rapid status changes, the fade timeout can queue. The `_currentCopyTarget` check helps but doesn't prevent the opacity transition from starting. Clear the pending transition on each update.
- [x] **PERF-LLM-04** `local-llm-chat.js:1114-1150` -- ~~`renderSafeText` runs per-message and compiles regexes inline (`/^```/`, `/^\s*[-*]/m`, `/^\s*\d+\.\s/m`) for every block. Pre-compile these regexes as module-level constants for reuse across all `renderSafeText` calls.~~ **Done.** Extracted 5 regex constants (`RE_RENDER_CODE_FENCE`, `RE_RENDER_UNORDERED_LIST`, `RE_RENDER_UNORDERED_LINE`, `RE_RENDER_ORDERED_LIST`, `RE_RENDER_ORDERED_LINE`) to module level and replaced all inline usages in `renderSafeText`.
- [ ] **PERF-LLM-05** `local-llm-worker.js:134-152` -- `TextStreamer` posts a `postMessage` for every token. Each cross-thread message involves serialization. Batch token messages: accumulate tokens in a small buffer (e.g., 4 tokens or 100ms) and post once per batch, reducing cross-thread overhead.
- [ ] **MEM-LLM-01** `local-llm-chat.js:701-706` -- `appendAssistantToken` uses `this.assistantDraft.content += token` (string concatenation). For a 5000-token response this creates 5000 intermediate strings. V8 optimizes this well in most cases, but for extreme outputs consider `Array#push` + `Array#join`.
- [ ] **MEM-LLM-02** `local-llm-worker.js:6-7` -- `pastKeyValuesCache` is a `DynamicCache` that persists across generations. Each generation reuses the same cache, accumulating key-value states. For a 10-message conversation, the cache grows with every new generation without incremental cleanup. `resetChatState` disposes it, but mid-conversation there's no pruning. Memory can grow unbounded.
- [ ] **CQ-LLM-01** `local-llm-chat.js` (1476 lines) -- The `LocalLlmUtility` class handles UI rendering, worker communication, state management, markdown rendering, LaTeX parsing, and mock worker simulation. Extract the renderer functions (`renderSafeText`, `LocalLlmLatexParser`, `escapeHtml`, `cleanupModelText`, `splitInlineLocalLlmSegments`, etc.) into `js/local-llm-renderer.js`. The chat class should focus on orchestration.
- [ ] **CQ-LLM-02** Code duplication between `js/local-llm-chat.js` and `utilities-src/src/localLlmState.ts`: Both implement `cleanupModelText`/`cleanupLocalLlmText`, `renderSafeText`/`renderLocalLlmSafeText`, `renderMarkdownInlineText`, `splitInlineLocalLlmSegments`, `findClosingInlineMathDelimiter`, `isEscapedAt`, `renderLocalLlmMath`, `LocalLlmLatexParser`, and `escapeHtml`. The TS file exports some functions publicly, while the JS file keeps them module-private. Verify whether `localLlmState.ts` is compiled into the final bundle and used by `local-llm-chat.js`, or if they're independent implementations. Consolidate.
- [ ] **CQ-LLM-03** `local-llm-chat.js:48-78` -- The `LocalLlmUtility` constructor initializes 30+ instance properties before calling `mount()` and `bindEvents()`. Extract property initialization into a `_initDefaults()` method or use private fields for encapsulation.
- [ ] **CQ-LLM-04** `local-llm-chat.js:782-785` -- `renderMessages` uses `innerHTML` with the output of `renderSafeText`. While `renderSafeText` only emits safe HTML (via `escapeHtml` on all text content), using `innerHTML` for user-generated content is inherently risky. Consider using `DOMParser` or `<template>` element parsing to validate the HTML structure before injection.
- [ ] **CQ-LLM-05** `local-llm-chat.js:863-882` -- `resetChat` fires `this.worker?.postMessage({ type: 'reset' })` and `void this.clearBrowserModelCaches()` in parallel. The cache clearing is fire-and-forget and may outlive the reset. If the user immediately sends a new message, cache eviction and model reload can race. This is benign but the cache clearing on every "Reset" button press is surprising -- clearing the cached model means the user must re-download ~290MB. Consider separating "Clear Conversation" from "Clear Model Cache".
- [ ] **CQ-LLM-06** `local-llm-chat.js:1071-1093` -- `buildFailureCopy` falls back to generic error copy when `this.diagnostics` is null (worker crashed before sending capabilities). This is correct but lowers error message quality. Log the missing diagnostics for debugging.
- [ ] **CQ-LLM-07** `local-llm-chat.js:1435-1442` -- The `escapeHtml` function in `local-llm-chat.js` and `escapeHtml` in `localLlmState.ts` are identical. If CQ-LLM-02 (code duplication) is resolved, ensure there's a single source of truth.
- [ ] **CQ-LLM-08** `local-llm-chat.js:26-35` -- `LOAD_CONTROL.idle` has `{ text: 'Load', disabled: false }` but no `cls` property, while all other states have `cls`. The CSS handling in `updateLoadControl` (line 487) is defensive (`if (appearance.cls)`) but the inconsistency is a maintenance hazard.
- [ ] **CQ-LLM-09** `local-llm-chat.js:38-46` -- NOTE: Spinner writes to `this.startText.textContent` (the Load button text). Loading sequence writes to `this.loadCopy.innerHTML` (the center panel paragraph). These are **different elements**. They don't conflict or override each other. The TODO's claim of conflict is incorrect. No action needed.
- [ ] **CQ-LLM-10** `local-llm-chat.js:821-828` -- `announceLastAssistantMessage` writes the full assistant message content to the `aria-live` region. For a 2000-character response, screen readers announce the entire text. Truncate to ~200 characters or use `aria-relevant="additions"` with incremental updates.
- [ ] **CQ-LLM-12** `local-llm-chat.js:952-971` -- `terminateWorker` sends `dispose` then schedules `worker.terminate()` after `delayMs` (800ms by default). The 800ms delay gives the worker time to process the dispose message and clean up GPU resources. Consider reducing to 400ms, which is still sufficient for the worker's synchronous cleanup.
- [ ] **CQ-LLM-13** `css/local-llm-chat.css:12-14` -- CSS custom properties `--local-llm-accent`, `--local-llm-good`, `--local-llm-danger` use hardcoded color values (#00e5ff, #8fd4a8, #f0a090). These should align with the design system if defined elsewhere in the project.
- [ ] **CQ-LLM-15** `css/local-llm-chat.css:499-503` -- `.local-llm-send--stop` references `--color-text-primary` which is not defined in `local-llm-chat.css`. It's inherited from the parent stylesheet. Use `--local-llm-text` for self-contained styling.
- [ ] **CQ-LLM-16** `css/local-llm-chat.css:655-661` -- On screens <=760px, `.local-llm-send-text` is visually hidden via the screen-reader-only technique, but `aria-label` on the button stays "Send message" and never changes to "Stop message" when in stop mode. Update `aria-label` dynamically in `updateStatus` (line 457-458).
- [ ] **TS-LLM-01** `local-llm-chat.js` is plain JavaScript (1476 lines). The companion `localLlmState.ts` is TypeScript. If `local-llm-chat.js` is compiled from TypeScript, verify the source-of-truth file. If it's standalone JS, consider migrating to TypeScript for type safety on the large class.
- [ ] **TS-LLM-02** `localLlmState.ts:38` -- `cleanupLocalLlmText` accepts `unknown` and coerces via `String(text || '')`. While `null`/`undefined` produce `""`, `String(0)` produces `"0"` and `String(false)` produces `"false"`. These are technically valid but indicate a type mismatch at the call site. Narrow the parameter type to `string | null | undefined`.
- [ ] **TS-LLM-03** `localLlmState.ts:47` -- `renderLocalLlmSafeText` accepts `unknown` for `markdown`. The `cleanupLocalLlmText` call handles the coercion, but a typed parameter (`string`) with a documented `null`/`undefined` guard is cleaner.
- [ ] **TS-LLM-04** `localLlmState.ts:87-90` -- The `InlineLocalLlmSegment` type is not exported. If `splitInlineLocalLlmSegments` or `renderInlineLocalLlmText` are used by external modules in the future, the return type is opaque. Export the type.
- [ ] **TS-LLM-05** `localLlmState.ts:92-119` -- `LATEX_COMMANDS` is not exported but has a duplicate in `local-llm-chat.js` (lines 1153-1180). If these are independent copies, they can drift. Consolidate by exporting from the TS module and importing in the JS file, or vice versa.
- [ ] **TEST-LLM-01** `localLlmState.test.ts` (87 lines, 8 tests) -- Missing tests for `cleanupLocalLlmText`: test `thinking` tags, special tokens, HTML entity handling, `null`/`undefined`/empty input, and multi-line content.
- [ ] **TEST-LLM-02** No tests for `LocalLlmLatexParser.test()`. Test: nested groups (`{{a}^{b}}`), subscript + superscript (`x_i^2`), `\frac{\sqrt{a}}{b}`, `\sqrt[3]{8}`, `\text{hello world}`, unknown commands, empty input, consecutive commands.
- [ ] **TEST-LLM-03** No tests for `compactLocalLlmMessages` with `notice` role messages. Verify that notices are preserved at the front and not counted toward `maxHistoryMessages`.
- [ ] **TEST-LLM-04** No end-to-end or integration tests for `LocalLlmUtility`. The `LocalLlmMockWorker` simulates the worker protocol, but the main thread controller is untested. Add jsdom-based tests for: load flow (idle -> checking -> loading -> ready), message send flow, interrupt flow, error recovery, cache clearing.
- [ ] **TEST-LLM-05** `localLlmState.ts` -- NOTE: There ARE existing tests for `renderLocalLlmSafeText` (emphasis at line 60-62, ordered lists at 56-58, LaTeX at 64-79, code spans/fences at 81-86, link escaping at 46-54). MISSING: bold (`**text**`), strikethrough, unordered lists, display math edge cases, mixed block types, empty input, HTML injection attempts.
- [ ] **TEST-LLM-06** No tests for `escapeHtml`: verify `&`, `<`, `>`, `"`, `'` are all escaped. Test numeric, boolean, null, undefined, and already-escaped input.
- [ ] **TEST-LLM-07** No tests for `buildFailureCopy`: verify that each error category (`unsupported-browser`, `download-failed`, `out-of-memory`, `runtime-import`, `runtime-failed`) produces distinct `title`, `detail`, and `likelyFix` messages.
- [ ] **TEST-LLM-08** No tests for `LocalLlmLatexParser.readRawBraceGroup` with deeply nested braces (`{{{{{a}}}}}`). Verify depth tracking works correctly and unclosed braces don't hang.
- [ ] **TEST-LLM-09** No tests for `splitInlineLocalLlmSegments` with adjacent inline code and math (`$x^2$`), escaped math delimiters (`\$not math\$`), and nested backticks.
- [ ] **TEST-LLM-10** `local-llm-chat.js:1104-1112` -- NOTE: `compactMessagesForWorker` does NOT prepend system prompt—the worker's `compactMessages` does. Tests should verify: notice filtering, content cleaning, truncation to `maxMessageChars`, empty content removal. System prompt prepending is not this function's responsibility.
- [ ] **API-LLM-01** `local-llm-chat.js` -- `LocalLlmUtility` is not exported. The singleton is created by `initLocalLlmUtility` on DOM ready. If other modules need to interact with the utility programmatically (e.g., pre-load, send a message), they can't. Consider a module-level getter or event bus.
- [ ] **API-LLM-02** `local-llm-chat.js` -- Public methods (`startChat`, `resetChat`, `sendMessage`, `interruptGeneration`, `clearModelCache`, `dispose`) have no JSDoc comments or return type documentation. Add JSDoc for external consumers and for the `initLocalLlmUtility` bootstrap.
- [ ] **API-LLM-03** `local-llm-config.js:24` -- `LOCAL_LLM_CONFIG.runtime.packageVersion` is hardcoded to `'4.2.0'`. If `moduleUrl` is updated but this version string isn't, the metadata is stale. Derive it at runtime from the `moduleUrl` or maintain a single constant.
- [ ] **API-LLM-04** `local-llm-worker.js:243-250` -- NOTE: The sanitization logic (filter notices, clean text, truncate) IS identical between main thread's `compactMessagesForWorker` (JS line 1104-1112) and worker's `compactMessages` (worker line 243-250). However, the worker adds: history slicing to `maxHistoryMessages`, stripping leading assistant messages, and prepending system prompt. These are different responsibilities, not full duplication. Consider extracting just the sanitization step.
- [ ] **COMP-LLM-01** `local-llm-chat.js:130` -- The `<textarea>` auto-sizes up to 160px but has no `scrollIntoView` on focus. On mobile with a virtual keyboard, the input may be partially obscured. Add `this.input.focus({ preventScroll: true })` followed by a `scrollIntoView({ block: 'end' })` with a small delay for mobile.
- [ ] **COMP-LLM-02** `local-llm-chat.js:420-423` -- NOTE: The label DOES update dynamically. Line 430: `this.backend = message.backend || 'webgpu'`, and line 522: `this.backendLabel.textContent = this.backend === 'webgpu' ? 'WebGPU' : this.backend`. The worker currently always reports `'webgpu'`, but the main thread already handles dynamic values.
- [ ] **COMP-LLM-03** `local-llm-chat.js:180` -- `queueInputChromeUpdate` uses `requestAnimationFrame` with a guard (`if (this._inputFrameId) return`). Rapid typing produces one rAF per frame, so input chrome updates at 60fps. Correct, but if the previous rAF is still pending when new input arrives, the update is delayed by one frame (~16ms at 60fps). This is imperceptible but worth noting.
- [ ] **COMP-LLM-04** `local-llm-chat.js:61-62` -- `this.readySuggestionOrder` is shuffled in the constructor and rebuilt in `startPromptCycle`. The shuffle uses Fisher-Yates (correct), but rebuilding the array every 5 seconds is unnecessary. Cache and reuse the shuffled order.
- [ ] **COMP-LLM-05** `local-llm-chat.js:440-477` -- `updateStatus` toggles status chip, input disabled state, send button state/label, progress bar visibility, placeholder text, and load control appearance. It's the central state machine and runs frequently. Consider splitting into focused sub-methods (`updateStatusChip`, `updateComposerState`, `updateProgressBar`, `updatePlaceholder`) for easier testing and maintenance.
- [ ] **COMP-LLM-06** `local-llm-chat.js:114-119` -- `aria-valuenow` on the progress bar updates via `applyProgressBarValue`, but screen readers don't announce `aria-valuenow` changes automatically. Add `aria-live="polite"` to the progress bar container, or set `aria-valuetext` periodically (e.g., every 10%) for better screen reader support.
- [ ] **COMP-LLM-07** `local-llm-chat.js:102-105` -- NOTE: Duplicates CQ-LLM-05. Both flag that the Reset button calls `clearBrowserModelCaches()`. The "Reset" button (`#localLlmResetBtn`) calls `resetChat({ clearMessages: true })` which also clears the browser's cached model (~290MB). Split into "Reset Chat" (clear messages only) and "Clear Cache" (evict model from browser cache).
- [ ] **COMP-LLM-08** `css/local-llm-chat.css:184-196` -- `.local-llm-center` is center-aligned in a grid overlay. On very small screens (<=360px), the centered content with `max-height: 100%` and `overflow: hidden` may clip vertically. Add `overflow-y: auto` or adjust the max-height at the smallest breakpoint.
- [ ] **COMP-LLM-09** `local-llm-chat.js:1114-1150` / `localLlmState.ts:47-85` -- `renderSafeText` (JS) and `renderLocalLlmSafeText` (TS) handle markdown rendering, but are independent implementations. If both are compiled and shipped, they may diverge on edge cases. Confirm which is the source of truth and deprecate the other.
- [ ] **CFG-LLM-01** `local-llm-config.js:19` -- `LOCAL_LLM_CONFIG.model.sourceUrl` is exported but never referenced in the application code. Either display it in the UI (e.g., as a link to the HuggingFace model card) or remove it.
- [ ] **CFG-LLM-02** `local-llm-config.js:29-32` -- NOTE: The claim of ~2K-4K token context window for Bonsai 1.7B is speculative—verify against the actual HuggingFace model card (`onnx-community/Bonsai-1.7B-ONNX`). ONNX-converted WebGPU models may have different limits than the original. If valid, `maxHistoryMessages: 10` and `maxMessageChars: 2500` should be reduced.
- [ ] **CFG-LLM-03** `local-llm-config.js:44-49` -- `systemPrompt` instructs the model not to output thinking tags or special tokens, while `cleanupModelText` strips them as a safety net. Both layers are needed because the prompt can't fully prevent model output. Document this defense-in-depth strategy.
- [ ] **CFG-LLM-04** `local-llm-config.js:34-42` -- Generation parameters (`max_new_tokens: 512`, `temp: 0.5`, `top_k: 20`, `top_p: 0.9`, `penalty_repeat: 1.05`) are reasonable for a small local model but undocumented. Add comments explaining the trade-offs: why `temp=0.5` (reduced randomness for small models), why `top_k=20` (narrow sampling horizon), and the effect of `penalty_repeat=1.05` (mild repetition deterrent).
- [ ] **CFG-LLM-05** `local-llm-config.js:24-25` -- `LOCAL_LLM_CONFIG.runtime.packageVersion` (`'4.2.0'`) and `moduleUrl` (`'@huggingface/transformers@4.2.0'`) are separate fields that can drift. NOTE: Duplicates API-LLM-03. Same underlying issue: derive the version from the URL at module load, or define a single `transformersVersion` constant.
- [ ] **PERF-LLM-06** `local-llm-chat.js:562-577` -- `renderStatePanel` fades `loadCopy` text between status changes using inline `opacity` transitions and a 300ms timeout. On rapid status changes (CHECKING -> LOADING -> OPTIMIZING in quick succession), the `clearTimeout` on the copy timer is correct, but the CSS `transition: opacity 0.3s` means the visual fade can still overlap. Force `transition: none` on rapid updates, or use `will-change: opacity` for smoother compositing.
- [ ] **PERF-LLM-07** `local-llm-chat.js:1188-1244` -- NOTE: `splitInlineLocalLlmSegments` (line 1206-1245) does NOT compile block-detection regexes. Its character-by-character loop is intentional for detecting backtick and `$` delimiters. The `/^\s*[-*]/m` regex is in `renderSafeText`, already covered by PERF-LLM-04. This TODO overstates the concern.

---

# Retro Virtual Machine Utility Code Review TODOs

**Files under review:** `utilities-src/src/retroVmController.ts`, `utilities-src/src/retroVmConfig.ts`, `utilities-src/src/retroVmSupport.ts`, `utilities-src/src/retroVmTypes.ts`, `utilities-src/src/v86-shim.d.ts`, `utilities-src/tests/retroVmConfig.test.ts`, `utilities-src/tests/retroVmSupport.test.ts`

---

## High Severity

- [ ] **SEC-VM-01** `retroVmController.ts:557-575` -- `pasteClipboard` calls `navigator.clipboard.readText()` then `emulator.keyboard_send_text(text, 0)` with zero delay. An arbitrary clipboard payload (e.g., a 50K character paste) is sent into the guest kernel's keyboard buffer as a single burst. The guest may lock up, panic, or misinterpret the input stream. Enforce a character limit (e.g., 2048 chars) and warn on truncation.
- [ ] **SEC-VM-02** `retroVmController.ts:557-575` -- `pasteClipboard` sends clipboard contents as raw keyboard events into the guest OS. If the guest has a terminal with `sudo` active, a clipboard containing shell commands would be executed. This is inherent to the design but should be documented in the UI with a visible warning near the paste button.
- [ ] **BUG-VM-02** `retroVmController.ts:616-633` -- `autoAdvanceBootMenu` sends two Enter key presses: one immediately and one after 900ms. The 900ms delay is undocumented. If the boot menu requires only one Enter, the second press could interfere with guest init scripts. Document or parameterize the dual-press behavior.
- [ ] **BUG-VM-03** `retroVmController.ts:327-328` -- `beforeUnloadHandler` calls `destroySession()` on `pagehide`, but the `pagehide` event cannot be cancelled. The `destroy()` promise may not settle before the page unloads, leaking WebAssembly memory until tab close. Acceptable given `pagehide` semantics, but document the limitation.
- [ ] **BUG-VM-04** `retroVmController.ts:330-337` -- `keyDownHandler` only fires when `this.state !== 'fullscreen'`. During fullscreen, Escape is handled by the browser. The `fullscreenchange` handler on line 339 releases pointer lock. Verify the pointer lock release is reliable when Escape triggers both fullscreen exit and the intended pointer unlock.
- [ ] **MEM-VM-01** `retroVmController.ts:10-11` -- `import 'v86/build/v86-fallback.wasm?url'` is a side-effect import that fetches ~1.5 MB of fallback WASM at module load time. Users who never launch the VM pay this cost. Lazy-load the fallback WASM alongside the main wasm in `createEmulator()`.
- [ ] **CORRECTNESS-VM-01** `retroVmController.ts:676-690` -- `syncGuestFit` sets CSS custom properties `--vm-fit-scale`, `--vm-guest-width`, `--vm-guest-height` on `screenContainer.style`. These are cleaned up in the non-graphical branch (line 680-682), but on `dispose()` the variables persist if the last state was graphical. Clean up in `dispose()`.

## Medium Severity

- [ ] **TS-VM-01** `retroVmController.ts:505` -- `new V86(...) as unknown as V86` double-casts through `unknown`, circumventing the type system. Cast directly to `EmulatorLike` with a JSDoc annotation.
- [ ] **TS-VM-02** `retroVmController.ts:393` -- NOTE: Field-level guards exist: `parseBooleanFlag`/`parseRelayUrl` provide fallbacks for malformed values. However, `root.dataset as unknown as RetroVmDatasetConfig` is still an unsafe cast of `DOMStringMap` with no shape validation at the top level. Valid concern for the cast, but lower risk than originally stated.
- [ ] **TS-VM-03** `retroVmSupport.ts:37` -- NOTE: Theoretical only. In all standard environments, `window` implies `navigator`. Only possible via deliberate `delete navigator` on window prototype. `navigator.maxTouchPoints` has no null guard. Keep as low-priority awareness.
- [ ] **CQ-VM-01** `retroVmController.ts:34-82` -- `FakeRetroVm` is defined in the main source file, not in test infrastructure. Extract to a dedicated test fixture. The class uses `window.setTimeout` which isn't mockable when baked into the source module.
- [ ] **CQ-VM-02** `retroVmController.ts:35-36` -- `FakeRetroVm.bus.send` is a no-op. `RetroVmMouseBridge.updateButtons` calls `bus.send('mouse-click', ...)` which silently no-ops during tests, making mouse bridge click behavior untestable.
- [ ] **CQ-VM-03** `retroVmController.ts:88-295` -- `RetroVmMouseBridge` is a large inner class (80+ lines) with a well-defined responsibility. Extract to `retroVmMouseBridge.ts` for testability and SRP.
- [ ] **CQ-VM-04** `retroVmController.ts:125-295` -- NOTE: Constructor takes 5 un-parameterized callbacks (line 138). Additionally, `RetroVmMouseBridge` creates 7 closure-bound arrow handlers. Each closure retains a reference to the entire bridge instance for the listener lifetime. Memory is fine per-instance, but note for profiling.
- [ ] **CQ-VM-05** `retroVmController.ts:410-416` -- `RetroVmMouseBridge` is instantiated in the constructor before `init()`. If `init()` throws, the mouse bridge is orphaned with no way to call `detach()`. Defer construction to `init()`.
- [ ] **BUG-VM-05** `retroVmController.ts:448-458` -- `init()` returns early for unsupported environments, but `ResizeObserver`, `mouseBridge.attach()`, and global listeners were already attached before the guard. These are leaked because nothing calls `dispose()`. Move the guard earlier in `init()`.
- [x] **BUG-VM-06** `retroVmController.ts:438` and line 456 -- `applyRuntimeLabels()` is called twice in the supported path of `init()`. Remove the redundant call. **Done.** Removed the second call at line 456.
- [ ] **CORRECTNESS-VM-02** `retroVmController.ts:350-362` -- `onScreenSize` receives `[width, height, bpp]` from v86 but `syncGraphicalMode()` ignores the bpp hint and checks for a canvas element. If the canvas is CSS-hidden (e.g., in tests), `graphicalModeActive` is wrong.
- [ ] **BUG-VM-07** `retroVmController.ts:508-523` -- NOTE: Test canvas can leak on `dispose()` without prior reset, but parent container is removed during component unmount, so no WASM/memory leak occurs. Still, cleaning up on `dispose()` is good practice.
- [ ] **CORRECTNESS-VM-03** `retroVmConfig.ts:106-108` -- `cdromSizeBytes` (20,082,688) is hardcoded. If the ISO is remastered but the constant isn't updated, the emulator truncates or fails. Add a build-time assertion or automated size check.
- [ ] **CORRECTNESS-VM-04** `retroVmConfig.ts:19` -- `bootOrder: 0x210` is undocumented. A developer changing this value without understanding SeabIOS boot order bit layout could break boot. Add a code comment. NOTE: Duplicates CFG-VM-02 below.
- [ ] **CORRECTNESS-VM-05** `retroVmConfig.ts:103` -- `wasm_path` comes from `v86/build/v86.wasm?url`. If the bundler doesn't resolve the WASM URL at runtime (subdirectory deployment), boot fails with an opaque error. Add a pre-launch fetch probe.
- [ ] **BUG-VM-08** `retroVmSupport.ts:91-97` -- NOTE: The TODO says "rounds up" but `Math.round` (line 96) rounds to nearest: 500 bytes -> "0 KB", 600 bytes -> "1 KB". The fix of adding a raw bytes branch for values under 1024 is still valid.
- [ ] **BUG-VM-09** `retroVmSupport.ts:123-127` -- The idle state returns `'Runtime is idle.'` when `progress.loadedBytes === 0`. After reset, the user sees this generic message instead of reset-aware copy. The idle case ignores the `supportReason` parameter.
- [ ] **CORRECTNESS-VM-06** `retroVmSupport.ts:173-192` -- NOTE: `transitionRetroVmState` returns `'error'` unconditionally for the `'error'` event. However, `'launch'` event also transitions from error -> loading (line 178), so two recovery paths exist (`launch` and `reset`). The concern about missing `retry` event is still valid.
- [ ] **CORRECTNESS-VM-07** `retroVmSupport.ts:173-192` -- `transitionRetroVmState` has no `default` case. A new event type added to the union would cause `undefined` to be returned. Add `default: assertNever(event)`.
- [x] **CORRECTNESS-VM-08** `retroVmSupport.ts:120-170` -- `resolveRetroVmStatusView` has no `default` case. A new state added to `RetroVmState` wouldn't be caught by TypeScript. Add `default: assertNever(state)`. **Done.** Added `assertNever` helper and extracted `RetroVmEvent` type for `transitionRetroVmState`.
- [x] **BUG-VM-10** `retroVmController.ts:809` -- ~~`launchButton.disabled` includes `this.state === 'loading' || this.state === 'fullscreen'` but NOT `this.state === 'resetting'`. A user could relaunch during reset, creating a second emulator. Add `resetting` to the disable condition.~~ **Done.** Added `resetting` to the launch button disable condition.
- [ ] **CORRECTNESS-VM-09** `retroVmController.ts:476-496` -- `launch()` guards on `this.emulator` but between the guard and `setState`, rapid clicks could pass. Add a synchronous `isLaunching` boolean flag.
- [ ] **CQ-VM-06** `retroVmController.ts:323` -- `bootHintTimer` initializes to `0`. After `clearTimeout(0)`, the value remains `0`, indistinguishable from "never set". Use `null` as the default.

## Low Severity

- [ ] **CQ-VM-07** `retroVmController.ts:297-832` -- `RetroVmController` is 535 lines handling state machine, DOM sync, mouse bridging, fullscreen, clipboard, resize, and lifecycle. Extract DOM sync logic into a dedicated helper.
- [ ] **CQ-VM-08** `retroVmController.ts:327-348` -- Handler properties mix naming: `beforeUnloadHandler`/`keyDownHandler` (suffix) vs `onScreenSize`/`onDownloadProgress` (prefix). Standardize one convention.
- [ ] **CQ-VM-09** `retroVmController.ts:222` -- `safeScale = viewport.scale || 1` is dead code; line 217 already guards `viewport.scale <= 0`. Remove the fallback.
- [ ] **CQ-VM-10** `retroVmController.ts:242-244` -- `typeof event.movementX === 'number'` is always true (`movementX` defaults to 0). Remove the defensive check.
- [ ] **CQ-VM-11** `retroVmConfig.ts:45-58` -- `parseBooleanFlag` and `parseRelayUrl` are module-level but only used by `resolveRetroVmConfigFromDataset`. Nest them or make private.
- [ ] **CQ-VM-12** `retroVmTypes.ts:76-81` -- `RetroVmStatusView.chipClass` is always `utility-status-chip--${chipLabel.toLowerCase()}`. Derive it in the caller instead of returning both fields.
- [ ] **CQ-VM-13** `retroVmController.ts:253-276` -- `requestPointerLock` has a confusing try-within-try pattern. Extract to `requestPointerLockWithFallback`.
- [ ] **CQ-VM-14** `v86-shim.d.ts:48-62` -- The `V86` class shim declares `automatically`, `is_running`, `stop`, `run`, `restart` methods never used by the controller. The `EmulatorLike` interface is the real usage surface.
- [ ] **CQ-VM-15** `retroVmTypes.ts:2-3` -- `RetroVmNicType` and `RetroVmDnsMethod` are string literal unions that exactly duplicate what's in the `v86-shim.d.ts:V86NetDeviceConfig` type. The types could diverge. Re-export from the v86 shim rather than redefining.
- [ ] **CQ-VM-16** `v86-shim.d.ts:1-65` -- The `v86` module declaration is comprehensive but includes `V86Options.disable_mouse` which the controller sets to `true` (line 113). This disables v86's built-in mouse handling, which is correct since `RetroVmMouseBridge` handles input. Document this explicitly.
- [ ] **CQ-VM-17** `retroVmController.ts:97-295` -- `RetroVmMouseBridge` sends `mouse-absolute` and `mouse-delta` messages to the bus, but `bus.send` is fire-and-forget with no delivery confirmation. If the v86 bus drops messages during high event rates (e.g., rapid mouse movement), input is silently lost. This is inherent to the v86 architecture but worth noting.
- [ ] **TEST-VM-01** `retroVmConfig.test.ts` -- Only 3 tests. Missing: all-fields-populated dataset override, `cdromSizeBytes: null`, whitespace-padded boolean flags, whitespace-only relay URL, valid relay URL with `isRetroVmNetworkReady`.
- [ ] **TEST-VM-02** `retroVmSupport.test.ts` -- Missing: `hasWindow: false`, `hasWorker: false`, `error`/`unsupported` status views with custom reason, `transitionRetroVmState` from `error` (should stay error for non-reset events).
- [ ] **TEST-VM-03** No unit tests for `RetroVmController`. The 535-line class has zero coverage. Test: launch flow, reset flow, fullscreen lifecycle, mouse bridge, progress tracking, state transitions. Playwright covers only the happy path.
- [ ] **TEST-VM-04** No tests for `RetroVmMouseBridge`. Click capture, pointer lock, absolute/delta coordinate transformation, and button tracking are untested. Tightly coupled to controller -- refactor first.
- [ ] **TEST-VM-05** `retroVmSupport.test.ts:8-28` -- The mobile detection test sets coarse pointer + narrow screen + touch points simultaneously. Add a test for `innerWidth < 900` alone (with 0 touch points and fine pointer) to verify narrow screen alone triggers mobile detection.
- [ ] **TEST-VM-06** No test coverage for `__OD_RETRO_VM_TEST_MODE__` paths in unit tests. Test mode creates `FakeRetroVm`, installs canvas, bypasses pointer lock. Exercised by Playwright only.
- [ ] **TEST-VM-07** No tests for `FakeRetroVm`. Verify correct event emission order (`download-progress` -> `screen-set-size` -> `emulator-ready`), correct payload shapes, and that `destroy()` clears all listeners.
- [ ] **API-VM-01** `retroVmController.ts:297` -- `RetroVmController` has no JSDoc. Document lifecycle: construct -> `init()` -> `launch()` -> `reset()` -> `dispose()`.
- [ ] **API-VM-02** `retroVmConfig.ts:92-94` -- `isRetroVmNetworkReady` checks enabled + relay URL. Rename to `hasNetworkRelayConfigured` or `isNetworkRelayReady` for clarity.
- [ ] **API-VM-03** `retroVmConfig.ts:69-90` -- Consider providing `resolveRetroVmConfigFromElement(element: HTMLElement)` as a convenience over the dataset cast + resolve pattern.
- [ ] **COMP-VM-01** `retroVmController.ts:378` -- The placeholder is hidden in `onReady`. If `emulator-ready` never fires (silent emulator failure), the placeholder stays hidden forever. Add a boot timeout to re-show the placeholder.
- [ ] **COMP-VM-02** `retroVmController.ts:557-575` -- `pasteClipboard` requires secure context (HTTPS) and fresh user activation. Document the HTTPS requirement near the paste button.
- [ ] **COMP-VM-03** `retroVmController.ts:721-746` -- `applyInteractionStatusCopy` has 3 early returns and 4 branches with repetitive status text. Extract to a `RetroVmStatusCopy` configuration object.
- [ ] **COMP-VM-04** `retroVmController.ts:760-769` -- `setCaptureState` hardcodes badge text for three states. Extract these labels to `RetroVmCopyConfig` for consistency.
- [ ] **COMP-VM-06** `retroVmController.ts:676-690` -- `syncGuestFit` computes fit-to-container scale via JS and CSS custom properties. Consider whether `object-fit: contain` on the canvas would eliminate the JS-driven CSS variable updates.
- [ ] **COMP-VM-07** `retroVmController.ts:420-431` -- Button handlers use `void this.launch()` (fire-and-forget). If the method throws synchronously, the error is unhandled. Add `.catch()` or inline `try-catch`.
- [ ] **COMP-VM-08** `retroVmController.ts:306 and line 340` -- `fullscreenButton` is required but `fullscreenchange` handler (line 339) runs on `document`. If the user opens the browser's dev tools or another fullscreen element outside the VM, the handler fires and calls `queueGuestFitSync()` unnecessarily. Harmless but noisy.
- [ ] **CFG-VM-01** `retroVmConfig.ts:17-18` -- `memorySize: 256 MB` and `vgaMemorySize: 8 MB` are hardcoded. Document min/max practical values. v86 caps guest RAM at ~2GB and VGA RAM at 128MB.
- [ ] **CFG-VM-02** `retroVmConfig.ts:19` -- `bootOrder: 0x210` has no comment. Document which bits map to CD-ROM, HDD, network, in SeabIOS encoding. NOTE: Duplicates CORRECTNESS-VM-04 above.
- [ ] **CORRECTNESS-VM-10** `retroVmConfig.ts:74-82` -- `resolveRetroVmConfigFromDataset` spreads default config then overrides per-property. A new `RetroVmCopyConfig` property won't be overridable without being explicitly listed. Add a lint rule, test, or derive overrides programmatically.
- [ ] **SEC-VM-03** `retroVmConfig.ts:38` -- `relayUrl` is configurable via dataset attribute. A malicious relay URL exposes guest traffic. Document that the relay URL must be trusted and not user-supplied.
- [ ] **SEC-VM-04** `retroVmController.ts:593` -- NOTE: `reset()` clears `screenContainer.innerHTML` after `destroySession()`. Current ordering in `reset()` is correct (line 586 calls `destroySession` before line 593 clears innerHTML). Fragile if refactored, but no action needed now.
- [ ] **CORRECTNESS-VM-11** `retroVmController.ts:259-262` -- In test mode, `requestPointerLock` sets `this.pointerLocked = true` synchronously. Real pointer lock is asynchronous (waits for `pointerlockchange`). The test timing doesn't match production.
- [ ] **CORRECTNESS-VM-12** `retroVmController.ts:441-446` -- `typeof ResizeObserver !== 'undefined'` is always true in browsers that support `pagehide` and `pointerlockchange`. The guard is dead code for this code path but harmless.
- [ ] **CORRECTNESS-VM-13** `retroVmController.ts:278-294` -- `releasePointerLock` has a `window.__OD_RETRO_VM_TEST_MODE__` guard (line 279) that synchronously sets state, plus a second guard (line 286) that resets state if `!this.pointerLocked || document.pointerLockElement !== this.root`. The second guard also fires in normal production when pointer is already unlocked, causing unnecessary `syncAbsoluteMouseMoveListener()` and `onCaptureStateChange(false)` calls. Deduplicate or short-circuit.
- [ ] **CORRECTNESS-VM-14** `retroVmController.ts:339-343` -- NOTE: Description inaccurate—condition `!document.fullscreenElement` only fires when fullscreenElement IS null, not when it's a different element. However, guarding against unintentional state reset for other reasons is still worth considering.
- [ ] **CORRECTNESS-VM-15** `retroVmController.ts:616-634` -- `autoAdvanceBootMenu` calls `wait_until_vga_screen_contains` with a 30s timeout, then sends Enter key presses. If the boot menu appears but the prompt text doesn't match `config.bootMenuPrompt` (`/Press ENTER to boot/i`), the function falls through silently and the guest hangs at the boot menu. Add a fallback timeout-based Enter dispatch.
- [x] **PERF-VM-01** `retroVmController.ts:641-648` -- ~~`syncGraphicalMode` calls `getComputedStyle(canvas).display` on every resize/fit sync. `getComputedStyle` triggers a forced synchronous reflow. Use `canvas.offsetParent !== null` or `canvas.hidden` for a cheaper visibility check.~~ **Done.** Replaced `getComputedStyle(canvas).display !== 'none'` with `!canvas.hidden && canvas.offsetParent !== null` to avoid forced reflow.
- [ ] **PERF-VM-02** `retroVmController.ts:780-816` -- `syncUi` runs on every state change, progress update, and is called from `setState`, `launch`, `reset`, `onDownloadProgress`, `onReady`. The method updates 15+ DOM properties. During rapid progress updates (~100ms intervals), this generates significant layout thrash. Batch DOM writes or use `requestAnimationFrame` to coalesce updates.
- [ ] **BUG-VM-11** `retroVmController.ts:692-701` -- `queueGuestFitSync` uses a single `requestAnimationFrame` guard via `resizeFrameId`. If `resize` fires before the rAF callback runs, the second request is dropped. If the container is resized significantly in the dropped frame, the fit won't update until the next rAF cycle. This is acceptable, but the dropped resize could cause a one-frame visual glitch.

---

# Stress Test Utility -- Code Review TODOs

**Files under review:** `utilities-src/src/stressTestController.ts`, `utilities-src/src/stressTestCore.ts`, `utilities-src/src/stressTest.worker.ts`, `utilities-src/src/stressTestWorkerTypes.ts`, `utilities-src/tests/stressTestCore.test.ts`, `pages/utilities/index.html` (stress-test section), `scripts/utilities-check.js` (stress-test e2e tests)

---

## High Severity

- [x] **BUG-ST-01** `stressTestController.ts:372-377` -- ~~Worker `error` event handler discards the actual error. The callback `() => { this.stopCpuStress(requestId); ... }` receives an `ErrorEvent` but never reads `event.message`, `event.filename`, or `event.lineno`. The error detail is lost and the user sees only "A CPU stress worker failed." Accept the `ErrorEvent` parameter and include `event.message` in the status message for debugging.~~ **Done.** Added `ErrorEvent` parameter, `console.error` logging with message/filename/lineno, and conditional user-facing message using `event.message`. All 133 tests pass.
- [x] **BUG-ST-02** `stressTestController.ts:620` -- ~~WebGPU device loss is never handled. `device.lost` event is never listened to. If the GPU driver resets, the device is lost due to memory pressure, or the tab is backgrounded long enough for the browser to reclaim GPU resources, all subsequent `requestAnimationFrame` frames will silently fail with uncaught exceptions from `encoder.beginComputePass()`, `device.queue.submit()`, etc. Add `device.lost.then((info) => { /* handle */ })` to report the loss gracefully.~~ **Done.** Added `device.lost` handler that cancels the animation frame, stops all stress workloads (CPU workers, GPU stress, metrics, visuals), sets error state with device loss reason, and syncs metrics. Also added `lost` property to `WebGpuDeviceLike` interface.
- [x] **BUG-ST-03** `stressTestController.ts:422-430` -- ~~`handleWorkerMessage` treats the fallback `else` branch (unrecognized message type) as an error, setting `this.lastGpuError = message.message`. But the `message` object is typed as `StressTestWorkerResponse`, and only `CpuStressErrorResponse` has a `message` property. For unrecognized message types, `message.message` is `undefined`, making `lastGpuError` the string `"undefined"`. Add a type guard to check `message.type === 'cpu-stress-error'` before accessing `message.message`.~~ **Done.** Added explicit type guard for `cpu-stress-error` before accessing `message.message`. Unrecognized message types now produce a meaningful error message (`Unexpected CPU worker message type: <type>`) instead of `"undefined"`.
- [ ] **BUG-ST-04** `stressTest.worker.ts:14-16` -- `chunkChannel.port1.onmessage` handler uses `scheduledChunks.shift()?.()` which drops the chunk if `shift()` returns `undefined` (empty array). The `MessageChannel` fires once per `postMessage` on `port2`. If the main thread dispatches a port message while the chunk callback is still executing (e.g., the 90ms work block hasn't finished but the next scheduled chunk's port message already queued), the port message arrives, `shift()` removes the first pending chunk, and execution resumes. This is correct in the single-threaded worker model but relies on the port message queue being FIFO. If the browser coalesces messages (it can for `MessageChannel`), chunks could be silently dropped, causing the worker to stall. Add a `while (scheduledChunks.length > 0)` loop to drain all pending chunks per port message.
- [ ] **MEM-ST-01** `stressTestController.ts:362-387` -- Workers accumulate event listeners on the main thread. Each worker gets two `addEventListener` calls (`message` and `error`), but `terminate()` doesn't automatically remove listeners registered on the main thread. While the Worker object is garbage collected (and listeners with it) after `terminate()`, there's a window during which the dead worker's message handler is still registered. In `handleWorkerMessage`, the `record.stopped` guard on line 397 handles this, but it would be cleaner to call `worker.removeEventListener` before `terminate()`. Store listener references for explicit cleanup.

## Medium Severity

- [ ] **BUG-ST-05** `stressTestController.ts:821-837` -- `stopGpuStress` calls `cancelAnimationFrame`, destroys WebGPU objects, and deletes WebGL shaders/buffers, but never calls `gl.makeContextLost()` (via `WEBGL_lose_context`) on the WebGL context. The canvas still holds a WebGL context after `dispose()`, preventing the browser from reclaiming GPU memory. After deleting resources, explicitly lose the context.
- [ ] **BUG-ST-06** `stressTestController.ts:451-464` -- `canCreateContext` uses `preserveDrawingBuffer: true` to check WebGL support, but the actual stress test context acquired in `getWebGlContext` doesn't require `preserveDrawingBuffer`. The `canCreateContext` probe creates and immediately discards a temporary canvas (correct), but the `preserveDrawingBuffer` flag on the probe may fail on some mobile GPUs where preserveDrawingBuffer is unsupported, causing the probe to return `false` even though the context works fine without it. Omit `preserveDrawingBuffer` from the probe options.
- [ ] **CORRECTNESS-ST-01** `stressTestController.ts:638-678` -- The WebGPU `frame()` loop reads `this.startedAt` (from the main `start()` call) to compute time uniform. However, `this.startedAt` is set in `start()` before `startGpuStress()` resolves, meaning the GPU timer starts at wall clock time, not when the GPU actually begins rendering. For `both` mode this is fine (timer aligns with CPU start), but for `gpu` mode where the adapter/device request may take seconds, the visual output starts with a non-zero time offset. Track GPU start time separately in `ActiveWebGpuStress.startedAt`.
- [ ] **CORRECTNESS-ST-02** `stressTestController.ts:781-809` -- The WebGL `frame()` loop also reads `this.startedAt` for the time uniform, with the same adapter acquisition delay issue as WebGPU. Store `startedAt` on `ActiveWebGlStress` (already exists on that type but is set to `performance.now()` at construction, not at first frame).
- [ ] **CORRECTNESS-ST-03** `stressTestController.ts:1018-1028` -- `recordGpuFrame` uses a 34ms threshold to detect dropped frames and a 16.7ms target (60fps). On displays with different refresh rates (120Hz, 144Hz, variable refresh rate), the drop detection is incorrect. A 9.76ms frame (120Hz) would be misidentified as normal, but a 12ms frame at 120Hz wouldn't trigger drops. Consider using `performance.timing` or reading the actual display refresh rate, or at minimum document the 60fps assumption.
- [ ] **CORRECTNESS-ST-04** `stressTestController.ts:263-332` -- NOTE: After tracing the start/stop flow: if user stops during GPU init, `stop()` terminates CPU workers, increments `requestId`, then back in `start()` the `requestId !== this.requestId` guard returns early. Works but relies on `stop()` being called synchronously and incrementing `requestId` first. Fragile but currently correct.
- [ ] **TS-ST-01** `stressTestController.ts:149-185` -- The class has 24 private fields with no JSDoc. Document the responsibility of each property group: state management (`state`, `mode`, `requestId`), workers (`workers`), GPU (`gpu`, `gpuBackend`, `gpuWorkloadLevel`), timing (`startedAt`, `frameCount`, `droppedFrames`, `lastFps`, etc.), and visualization (`canvas`, `thermalNodes`, `canvas2dCtx`).
- [ ] **TS-ST-02** `stressTestController.ts:15-19` -- NOTE: The math is wrong. At 10M iterations/sec, `Number.MAX_SAFE_INTEGER / 10_000_000` is approximately 28.5 years, not ~57 days. The conclusion (not a practical concern) is correct, but the number is off by a factor of ~1800. `StressWorkerRecord` uses `iterations: number` but tracks cumulative iterations. `this.totalIterations` on line 404 accumulates the delta, which could overflow for long-running tests.
- [ ] **PERF-ST-01** `stressTestController.ts:905-1016` -- `renderCpuVisualsFrame` creates multiple `CanvasGradient` objects per frame: one `LinearGradient` per thermal node (up to 42 radial gradients), plus per-worker linear gradients and one linear background gradient. At 60fps this is 42+ gradient allocations per frame. Each `createRadialGradient`/`createLinearGradient` allocates GPU resources. Consider reusing gradient objects or rendering to an offscreen canvas and blitting.
- [ ] **PERF-ST-02** `stressTestController.ts:1048-1079` -- `syncMetrics` runs at 60fps via rAF but is gated by `METRIC_INTERVAL_MS` (250ms). However, the `queueControlPanelFitSync()` call on line 1079 fires every sync, queuing an rAF that calls `syncControlPanelFit()` which does `getBoundingClientRect`-triggering DOM reads. The `syncControlPanelFit` function modifies `card.hidden` which triggers layout. This causes forced reflows 4x per second. Throttle `queueControlPanelFitSync` or batch with the metric interval.
- [ ] **PERF-ST-03** `stressTestController.ts:960-965` -- The wave line rendering in `renderCpuVisualsFrame` iterates `xStep` by `Math.max(24, w / 36)` but creates a `moveTo` + many `lineTo` calls. On a 1920px canvas this is ~53 `lineTo` calls per worker lane. For 8 workers that's 424 `lineTo` calls in the wave section alone. Use `Path2D` and `ctx.fill()` for better batching.

## Low Severity

- [ ] **BUG-ST-07** `stressTestController.ts:839-856` -- `compileShader` error message on line 844 says "Unable to create WebGL2 shader" regardless of whether the context is WebGL1 or WebGL2. Dynamically reference the backend type.
- [ ] **BUG-ST-08** `stressTestController.ts:846-853` -- Same issue: shader compilation error message says "WebGL2 shader failed to compile" for both WebGL1 and WebGL2.
- [ ] **BUG-ST-09** `stressTestController.ts:742-750` -- When the WebGL program fails to link, `compileShader` calls `loseContext()` on line 747, and the error is thrown. However, `loseContext()` is only available via the `WEBGL_lose_context` extension, which is optional. If the extension is unavailable, the context persists with a broken program. The existing `if (loseContext)` guard handles this correctly, but `compileShader` (line 849) does the same pattern for shader compilation errors. Consolidate the lose-context logic into a shared helper.
- [x] **CORRECTNESS-ST-05** `stressTestCore.ts:63-86` -- `transitionStressState` has no `default` or `assertNever` for unrecognized events. A new `StressEvent` added to the union type would silently return the current state. Add `default: throw new Error(\`Unexpected event: ${event}\`)`. **Done.** Added `assertNever` helper to catch unexpected events at runtime.
- [ ] **CORRECTNESS-ST-06** `stressTestCore.ts:63-86` -- No transition exists from `error` or `unsupported` states back to `starting` via a `retry` event. The only way to recover is to transition to `idle` via `reset`. If a future feature adds a "Retry" button, add a `retry` event that maps `error` -> `starting`.
- [ ] **CORRECTNESS-ST-07** `stressTestWorkerTypes.ts:1-38` -- NOTE: `StopCpuStressRequest` (line 7-10) is missing `workerIndex`, but the worker's `stop()` handler (lines 75-85) only reads `request.requestId` and doesn't need `workerIndex` from the request. Purely a type completeness issue, not a runtime bug. Add `workerIndex?: number` for completeness.
- [ ] **CQ-ST-01** `stressTestController.ts:21-75` -- The WebGPU interface definitions (`WebGpuAdapterLike`, `WebGpuDeviceLike`, etc.) are extensive but use `unknown` for shader modules, bind groups, pipelines, etc. These could use more specific types or at minimum be in a separate file (e.g., `stressTestWebGpuTypes.ts`) to reduce the controller file size.
- [ ] **CQ-ST-02** `stressTestController.ts:122-126` -- `STRESS_METRIC_HIDE_ORDER` maps mode to metric hide order, but for `cpu` mode it hides `gpu`, `fps`, `dropped` (GPU-related metrics) first, and for `gpu` mode it hides `iterations`, `workers` (CPU-related) first. This is logically correct but the ordering within each mode is arbitrary between metrics of the same category. Document the hide priority rationale.
- [ ] **CQ-ST-03** `stressTestController.ts:128-132` -- `getStressTestMaxWorkersOverride` documents itself as an "internal debug hook" but is exposed as a global property on `window`. The naming convention (`__OD_...`) follows the project's convention for debug hooks, but consider guarding with a feature flag or only checking in development builds to avoid unintended use.
- [ ] **CQ-ST-04** `stressTestController.ts:313-318` -- The fallback message "CPU stress is running. GPU stress is unavailable in this browser." is only shown for `both` mode with no GPU. For `gpu`-only mode with no GPU, the state is set to `'unsupported'`. These are two different UX paths for the same underlying issue (no GPU). Consider unifying the messaging.
- [ ] **CQ-ST-05** `stressTestController.ts:895-903` -- `stopCpuVisuals` sets `this.cpuVisualFrameId = 0` after canceling the rAF. But `startCpuVisuals` checks `if (this.cpuVisualFrameId || this.reducedMotion)` on line 859. After `stopCpuVisuals`, calling `startCpuVisuals` again would work, but if `stop()` is called between successive `start()` calls in rapid succession, `this.cpuVisualFrameId` is `0` and `startCpuVisuals` would start a fresh loop. This is correct but the naming is confusing: `cpuVisualFrameId` serves dual purpose as both the frame ID and the "is running" boolean. Rename to `cpuVisualRunning` and `cpuVisualRAFId`.
- [ ] **CQ-ST-07** `stressTestController.ts:388-394` -- `stopCpuStress` ignores the `_requestId` parameter. It stops ALL workers regardless of which request ID called it. This is correct for the current design (all workers share the same lifecycle), but the parameter is misleading. Remove it or use it to guard against stopping workers from a different request.
- [ ] **CQ-ST-08** `stressTestController.ts:1155-1171` -- `prepareGpuCanvas` calls `replaceCanvasElement()` unconditionally, which replaces the canvas in the DOM even if a 2D context was already available. The 2D context is cleared (`this.canvas2dCtx = null`) and thermal nodes are discarded. This is correct but disruptive -- the canvas DOM replacement triggers reflow and destroys any pending drawing operations. Consider a lazy replacement that only happens if the 2D context can't be cleared.
- [ ] **CQ-ST-09** `stressTestController.ts:1144-1153` -- `resizeCanvas` clamps `devicePixelRatio` to 2 via `Math.min(window.devicePixelRatio || 1, 2)`. On 3x or 4x displays (Pixel, some iPads, future Macs), the canvas renders at half the native resolution, resulting in blurry visuals. Consider raising the cap to 3 or making it configurable.
- [ ] **CQ-ST-10** `stressTestController.ts:122-126` -- `STRESS_METRIC_HIDE_ORDER['cpu']` lists `['dropped', 'gpu', 'fps', 'workers', 'iterations', 'elapsed']`. This hides the `workers` metric in CPU mode, but the number of workers is the most relevant CPU metric. Consider keeping `workers` visible in CPU mode.
- [ ] **TS-ST-03** `stressTestController.ts:134-137` -- `getNavigatorGpu` casts `navigator` to `Navigator & { gpu?: WebGpuLike }`. The `gpu` property on Navigator is a non-standard API. The cast is correct for runtime safety, but the method name doesn't reflect the nullability. Consider renaming to `tryGetNavigatorGpu` or using the existing pattern of returning null on failure.
- [ ] **TS-ST-04** `stressTestController.ts:139-142` -- `getWebGpuUsageFlag` accesses `globalThis.GPUBufferUsage` via an unsafe cast. If the browser exposes `GPUBufferUsage` with different property names (e.g., different capitalization), the fallback to `0` produces a buffer with zero usage, causing WebGPU validation errors. Add a console warning when flags resolve to 0.
- [ ] **TEST-ST-01** `stressTestCore.test.ts` -- 6 test cases covering core helpers. Missing: `transitionStressState` from `error` state for all events, `transitionStressState` with `reset` event from all states, `resolveCpuWorkerCount` with negative `hardwareConcurrency`, `resolveCpuWorkerCount` with `NaN`, `formatStressElapsed` with negative values, edge cases at 86400s boundary (exactly 1 day), and `formatStressElapsed` with fractional milliseconds.
- [ ] **TEST-ST-02** No unit tests for `StressTestController`. The 1207-line class has zero test coverage. Key flows to test: start/stop lifecycle, mode switching, CPU worker management (mocked), GPU backend fallback, metric updates, error recovery, unsupported GPU handling, dispose cleanup, `syncControlPanelFit` behavior.
- [ ] **TEST-ST-03** No unit tests for `stressTest.worker.ts`. Test: chunk scheduling, heartbeat interval, stop handling, requestId validation, iteration counting, checksum computation determinism.
- [ ] **TEST-ST-04** `stressTestCore.test.ts:48-55` -- `transitionStressState` doesn't test the `reset` event or all state-event combinations. The state machine has 6 states and 7 events = 42 possible transitions. Add a matrix test to verify all transitions.
- [ ] **TEST-ST-05** No tests for `handleWorkerMessage` covering: heartbeat with decreasing `iterations` (should use `Math.max`), stopped message with stale `requestId`, unrecognized message type, error message handling.
- [ ] **COMP-ST-01** `stressTestController.ts:164` -- `reducedMotion` is set at construction time from `window.matchMedia('(prefers-reduced-motion: reduce)')`. This is a static snapshot that doesn't update when the user changes their system preference. Add a `MediaQueryList.addEventListener('change')` listener to update `reducedMotion` at runtime and optionally stop/start CPU visuals.
- [ ] **COMP-ST-02** `stressTestController.ts:796-798` -- WebGL uniform updates call `gl.uniform1f` and `gl.uniform2f` with potentially null location objects. `active.timeLocation` is typed as `WebGLUniformLocation | null`. If `gl.getUniformLocation` returns `null` (uniform was optimized away by the driver), the `uniform1f(null, value)` call is a no-op but generates a WebGL `INVALID_VALUE` error. Guard with null checks.
- [ ] **COMP-ST-03** `stressTestController.ts:450-530` -- WebGL context creation uses `powerPreference: 'high-performance'` which forces discrete GPU usage. On laptop Macs with integrated/Apple Silicon GPUs, this may trigger the dedicated GPU (if available) causing battery drain. This is intentional for a stress test, but document it.
- [ ] **COMP-ST-04** `stressTest.worker.ts:33-39` -- NOTE: The double modulo `((checksum % 1) + 1) % 1` is intentional, not redundant. A single `% 1` gives `[-1, 1)` range, but the code needs `[0, 1)` to handle negative values from `sin*cos`. The double modulo is the standard pattern for positive-floor-invariant modulo. The performance difference is negligible.
- [ ] **COMP-ST-05** `stressTestController.ts:812-819` -- `resolveWebGlWorkloadLevel` uses `this.lastFps < 45` as the threshold to stop increasing workload. This is hardcoded and assumes 60fps target. On a 120Hz display, `lastFps` of 90fps would not trigger the limit, causing the shader to ramp to max workload quickly. Compute threshold as `targetRefreshRate * 0.75`.
- [ ] **COMP-ST-06** `pages/utilities/index.html:832` -- The `<canvas id="stressCanvas">` has fixed `width="960" height="540"` HTML attributes. These are overwritten by `resizeCanvas()` on first frame, but may cause a brief visual flash at the wrong aspect ratio before the first `requestAnimationFrame`. Set initial dimensions via CSS or omit HTML attributes entirely.
- [ ] **API-ST-01** `stressTestController.ts:149` -- `StressTestController` has no JSDoc. Document lifecycle: construct with `root` -> `init()` -> optional `start()`/`stop()` -> `dispose()` on unmount. Document that only one instance should exist per utility shell.
- [ ] **API-ST-02** `stressTestCore.ts:63-86` -- `transitionStressState` is the only exported state machine function but `StressEvent` and `StressState` types aren't documented. Add JSDoc explaining the state diagram and valid transitions.
- [ ] **API-ST-03** `stressTestWorkerTypes.ts:1-38` -- Types are exported but not documented. `CpuStressErrorResponse.message` could include more context (stack trace, error type) for debugging worker crashes.
- [ ] **CFG-ST-01** `stressTestController.ts:117` -- `WEBGPU_STORAGE_ITEMS` is 262144 (1MB of f32 data). This is a fixed-size buffer that doesn't adapt to GPU capability. For GPU memory stress testing, this could be scaled based on available VRAM (accessible via `adapter.info` in WebGPU).
- [ ] **CFG-ST-02** `stressTestController.ts:118-120` -- `WEBGL_MAX_WORKLOAD_LEVEL` (6) and `WEBGL_WORKLOAD_ITERATIONS_PER_LEVEL` (64) define a max of 384 shader loop iterations. This is conservative for modern GPUs. Document the rationale or make it configurable.
- [ ] **SEC-ST-01** `stressTestController.ts:128-132` -- `getStressTestMaxWorkersOverride` accesses `window.__OD_STRESS_TEST_MAX_WORKERS__` which could be set by third-party scripts (ad blockers, extensions) or by the page itself. While this is an intentional debug hook, note that a malicious page context could use it to spawn excessive workers for a denial-of-service. The cap of `MAX_CPU_WORKERS` (64) in `resolveCpuWorkerCount` limits the blast radius.
- [ ] **PERF-ST-04** `stressTestController.ts:1079` -- NOTE: Overlaps with PERF-ST-02. `queueControlPanelFitSync` is called from `syncMetrics` which runs up to 4x/sec. The rAF-queued `syncControlPanelFit` reads `controlPanel.scrollHeight` and `controlPanel.clientHeight`, triggering forced reflows. Debounce or skip when metrics haven't meaningfully changed.
- [ ] **PERF-ST-05** `stressTestController.ts:913-914` -- CPU visuals rendering starts with `ctx.fillStyle = '#000000'` and `ctx.fillRect`, then overwrites with a gradient. The initial black fill on line 913 is painted over by the gradient `fillRect` on line 930. Remove the redundant black fill.
