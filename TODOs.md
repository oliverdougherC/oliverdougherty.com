# Website Code Review TODOs
Generated: Tuesday, May 19, 2026

## Home Page

- [ ] [Priority: HIGH] [Accessibility] `nav-dot` button has no visible text and relies entirely on `aria-label`. If JS fails or aria-label is not read by some screen readers, the button is invisible and unlabeled. Consider adding a visually-hidden text label (index.html:28)
- [ ] [Priority: HIGH] [Accessibility] The `nav-overlay` div is `display: flex` only when `.active` class is present. When the nav opens, there is no `autofocus` or focus-trap — keyboard focus can still reach page content behind the overlay. Trap focus inside the overlay when open (js/main.js:313-329)
- [ ] [Priority: HIGH] [Accessibility] Marquee band has `aria-hidden="true"` which hides animated text from screen readers, but provides no equivalent accessible text for the skills list it displays (index.html:75-79)
- [ ] NOTE: [Priority: MED] [Security] ~~`buildThemeToggleButton` and `bindThemeToggleButton` use `innerHTML` to set button content. While the content is hardcoded and not user-controlled, this pattern is fragile and could become a vector if refactored to accept dynamic content. Use `textContent` or `createElement` instead (js/year.js:94, js/year.js:107)~~ — False positive: innerHTML uses hardcoded template literals with no user data (`'<span class="theme-toggle-icon" aria-hidden="true"></span>'`). Not a security issue.
- [ ] [Priority: MED] [Performance] `initBlueprintWordmark` calls `renderOverlay` synchronously on DOMContentLoaded, then again on `document.fonts.ready`, then on every `ResizeObserver` callback. The ResizeObserver fires `requestAnimationFrame(renderOverlay)` without debouncing — rapid resizes can cause multiple overlay rebuilds per frame (js/main.js:230-236)
- [ ] [Priority: MED] [Performance] The starfield worker listener on `resize` (js/starfield.js:88-90) is not debounced — rapid resize events will flood the worker with messages. Wrap with a debounce or RAF throttling.
- [ ] [Priority: MED] [Performance] `initSmoothScroll` creates a custom `smoothScrollTo` animation loop via `requestAnimationFrame` with `window.scrollTo` per frame. Native `scrollIntoView({ behavior: 'smooth' })` exists and is hardware-accelerated in most browsers. Consider replacing the custom implementation (js/main.js:529-559)
- [ ] [Priority: MED] [Performance] `syncNavScrollState` fires on every scroll event and calls `nav.classList.toggle` which triggers style recalculation on every scroll pixel. Debounce or use IntersectionObserver for the `scrollY > 50` check (js/main.js:280-283, js/main.js:387)
- [ ] [Priority: MED] [Error Handling] `document.fonts.ready.then(renderOverlay).catch(() => {})` — empty catch block silently swallows font-loading errors (js/main.js:226)
- [ ] [Priority: MED] [Error Handling] `startWorkerRenderer` has a blanket `catch` that returns `false` on any error, which silently falls through to the main-thread fallback. Worker initialization failures are completely invisible in production (js/starfield.js:50-106)
- [ ] [Priority: MED] [Error Handling] `finishBlueprintAnimations` uses empty catch blocks (`catch {}`) when calling `anim.finish()`. This swallows errors from non-finite or unsupported animations with no logging (js/main.js:419-421)
- [ ] [Priority: LOW] [Code Quality] `schematic-mono` class sets `font-family: var(--font-interface)` which is Inter, not a monospace font. The class name implies monospace but the CSS uses the interface font. Either rename the class or update the font-family (css/schematic.css:373-376)
- [ ] [Priority: LOW] [Code Quality] Duplicate comment in design-system.css: `/* Light mode disabled on utilities page via data-disable-color-mode */` appears twice on consecutive lines 349-350 (css/design-system.css:349-350)
- [ ] [Priority: LOW] [Code Quality] `readStoredColorMode` function has a leading space on line 40 (` function readStoredColorMode`) — inconsistent indentation (js/year.js:40)
- [ ] [Priority: LOW] [Code Quality] The `schematic.css` overrides `html { color-scheme: light }` without scoping to `body.schematic-mode`. This affects the entire document regardless of whether `.schematic-mode` is active. The comment says the stylesheet is scoped, but the `html`/`body` selectors at lines 21-32 are global (css/schematic.css:21-32)
- [ ] [Priority: LOW] [Browser Compatibility] `color-mix()` is used extensively in design-system.css and schematic.css (line 275). Safari < 16.3 and older Chromium browsers don't support `color-mix()`. Provide fallback values for those browsers.
- [ ] [Priority: LOW] [Browser Compatibility] `Element.prototype.getAnimations()` used in `finishBlueprintAnimations` is not supported in Firefox < 122 and Safari < 16.4. The guard check handles this, but the fallback is to skip finishing entirely — may leave animations in an inconsistent state for older browsers (js/main.js:411)
- [ ] [Priority: LOW] [Accessibility] The `<img>` for blueprint-david.jpg has `alt="Michelangelo's David"` which is minimal. Consider a more descriptive alt text explaining the context (index.html:127)
- [ ] [Priority: LOW] [Code Quality] The `debounce` utility in main.js: the inner `clearTimeout` inside the `later` function is dead code — it's never reached because the outer `clearTimeout` already cleared it. Functionally correct but confusing (js/main.js:564-574)
- [ ] [Priority: LOW] [Performance] The `letterTiming` array in `initBlueprintWordmark` is hardcoded for exactly 10 characters (DOUGHERTY.) but falls back to the last entry for any extra characters. If the word changes, the animation timing would look wrong without updating this array (js/main.js:180-191)

## Resume Page

- [ ] [Priority: HIGH] [Code Quality / Dead Code] The entire "Autonomous Fox AI" feature is disabled but all code remains in the page. The inline `<style>` block forces `.cursor-fox { display: none !important; }` (line 93), and the JS block starts with `if (true) return; // FOX_DISABLED` (line 474). This means ~250 lines of dead code (inline styles lines 34-94, JS lines 471-722, HTML elements lines 26-32) is parsed and shipped on every page load. Remove or conditionally load. (pages/resume/index.html:34-94, 471-722, 26-32)
- [ ] [Priority: MED] [Code Quality] `<link rel="alternate" media="only screen and (max-width: 760px)" href="../../mobile/resume/">` is intended to redirect mobile users but `rel="alternate"` does not perform a redirect -- it only signals search engines about an alternate version. Mobile visitors are served the full desktop page with no actual redirect. Consider a meta refresh, JavaScript redirect, or server-side User-Agent detection if mobile redirect is desired. (pages/resume/index.html:8)
- [ ] [Priority: MED] [Accessibility] `nav-dot` button has no visible text content, relying entirely on `aria-label="Open menu"`. If a screen reader fails to read the aria-label or JS is disabled, the button is completely unlabeled. Add a visually-hidden `<span class="sr-only">Open menu</span>` inside the button. (pages/resume/index.html:96)
- [ ] [Priority: MED] [CSS] Duplicate rule: `.coursework-list li:hover { border-color: var(--resume-accent); }` is defined twice with identical content on consecutive lines. (css/resume.css:436-442)
- [ ] [Priority: MED] [Accessibility] SVG icons inside `.section-heading` elements have `.heading-icon { display: none; }` set in CSS (line 321), meaning all section heading SVGs (Education, Projects, Experience, etc.) are hidden from sighted users AND screen readers simultaneously. They serve no purpose and waste DOM parsing. Remove from HTML or make them visible decorative icons with `aria-hidden="true"`. (css/resume.css:321, pages/resume/index.html:162-167, 194-200, 244-250, 271-275, 317-322)
- [ ] [Priority: LOW] [Performance] Missing Open Graph and Twitter Card meta tags. Social sharing of the resume page will have no preview title, description, or image. (pages/resume/index.html:6-7)
- [ ] [Priority: LOW] [CSS] `78svh` used in responsive media queries (line 751). The `svh` unit is not supported in Safari < 16.4, older Chromium, and Firefox < 113. Provide a `vh` fallback via `min-height: 78vh; min-height: 78svh;`. (css/resume.css:751)
- [ ] [Priority: LOW] [Performance] The typing animation uses `await new Promise(r => setTimeout(...))` in a chain of async steps. If the user navigates away mid-animation, the pending promises are orphaned. Not a memory leak in practice (the page unloads), but consider an AbortController pattern for cleanliness. (pages/resume/index.html:385-428)
- [ ] [Priority: LOW] [Code Quality] Inline style `color:#2BA84A` on line 120 duplicates the CSS custom property `--resume-accent` defined in resume.css. Use a CSS class or `var(--resume-accent)` instead of hardcoding the hex value. (pages/resume/index.html:120)
- [ ] [Priority: LOW] [Code Quality] `cursorDot` and `cursorCircle` divs at end of body (lines 725-726) reference the custom cursor system but no associated JS or meaningful behavior exists on this page when the fox is disabled. These empty elements serve no purpose. (pages/resume/index.html:725-726)
- [ ] [Priority: LOW] [CSS] No print media query styles. A resume page is a natural candidate for print optimization (remove decorative elements, ensure legible contrast, hide nav). (css/resume.css)

## Photo Gallery Page

- [ ] NOTE: [Priority: HIGH] [Accessibility] ~~Hero image has empty `alt=""` on initial load~~ — False positive: alt="" is dynamically populated by JS (`gallery.elements.heroImage.alt = entry.displayTitle` in syncHeroFeature at line 317). Matches exemption for images dynamically populated by JS.

- [ ] NOTE: [Priority: HIGH] [Accessibility] ~~Lightbox image also has empty `alt=""` on initial load~~ — False positive: alt="" is dynamically populated by JS (`gallery.elements.lightboxImage.alt = entry.displayTitle` in renderLightboxEntry at line 621). Matches exemption for images dynamically populated by JS.

- [ ] [Priority: HIGH] [Accessibility] `photo-card-button` removes focus outline via `outline: none` but provides no visible focus ring replacement. The parent `.photo-card:focus-within` has a `translateY(-3px)` transform which is subtle and may not be perceivable as a focus indicator on its own. Add a visible focus ring (e.g., `box-shadow: 0 0 0 2px var(--lb-accent)`) to `.photo-card-button:focus-visible`. (css/gallery.css:336-338)

- [ ] [Priority: MED] [Security] `window.__galleryState` exposes internal gallery state and `openPhotoById` / `closeLightbox` functions on the global `window` object. This creates an attack surface where injected scripts (e.g., via XSS in linked resources) can manipulate gallery state. If not needed for developer debugging, remove this global exposure. (js/gallery.js:135-141)

- [ ] [Priority: MED] [Performance] `navigateLightbox()` uses `setTimeout(..., 150)` to delay image swap, but rapid keyboard/touch navigation can stack multiple pending timeouts. Navigating left-right-left in under 450ms creates a queue of stale state mutations that fire out of order. Use an AbortController or cancel flag to discard in-flight navigations. (js/gallery.js:711-727)

- [ ] [Priority: MED] [Performance] `fetchJson()` always passes `{ cache: 'no-store' }` to `fetch()`. The manifest and sequence JSON files are static assets that rarely change -- this forces a full network round-trip on every page load instead of using the browser's HTTP cache. Consider `cache: 'no-cache'` (revalidates with etag) or `cache: 'force-cache'` with version query params. (js/gallery.js:170)

- [ ] [Priority: MED] [Performance] `handleHashChange()` fires on every `hashchange` event with no throttling. Rapid hash changes (e.g., back/forward button mashing) can trigger multiple sequential `openLightboxById` calls before the previous one completes. Add a guard flag or debouncing. (js/gallery.js:809-823, 88)

- [ ] [Priority: MED] [Performance] `document.addEventListener('keydown', handleGlobalKeydown)` is registered in `bindStaticEvents()` but never removed. If `initGallery()` fails and the page is kept alive (e.g., in a SPA or iframe), the listener persists and holds a reference to the `gallery` object, preventing GC. Add a cleanup mechanism or store the reference for removal. (js/gallery.js:87)

- [ ] [Priority: MED] [Performance] `document.querySelectorAll('[data-lightbox-dismiss]')` on line 83 searches the entire document for dismiss targets. If other components use the same `data-lightbox-dismiss` attribute, they would incorrectly close the gallery lightbox. Scope the selector to `gallery.elements.lightbox.querySelectorAll(...)` instead. (js/gallery.js:83-85)

- [ ] [Priority: MED] [Performance] `IntersectionObserver` created in `initScrollReveal()` is never disconnected. If `renderGallery()` is called again (e.g., via a future refresh feature), a new observer would be created without cleaning up the old one. Store the observer reference and call `observer.disconnect()` before creating a new one. (js/gallery.js:350-372)

- [ ] [Priority: MED] [Error Handling] Image `error` event handler on photo cards silently applies `photo-card--broken` class with no logging or user notification. If the gallery server returns 404s, the user sees blank cards with no indication of failure. Log the failed image URL and consider showing an error placeholder. (js/gallery.js:490-493)

- [ ] [Priority: MED] [Error Handling] `navigateLightbox()` has no guard for `gallery.currentIndex` being `-1`. If called before `gallery.currentIndex` is set (e.g., via `openLightboxById` failing), `(gallery.currentIndex + direction + length) % length` computes with `-1` which produces an off-by-one index. (js/gallery.js:711-727)

- [ ] [Priority: LOW] [Code Quality] `formatDateRange()` function is defined but never called anywhere in the codebase. Dead code -- remove or integrate into `buildLightboxMeta` if intended for future use. (js/gallery.js:955-977)

- [ ] [Priority: LOW] [CSS] Duplicate `.lightbox-picture` rule block: lines 609-615 and 617-621 define the same selector with overlapping `display: flex`, `align-items: center`, `justify-content: center` properties. Merge into a single rule. (css/gallery.css:609-621)

- [ ] NOTE: [Priority: LOW] [CSS] ~~`!important` used on `--reveal-delay: 0ms !important`~~ — False positive: !important used in responsive mobile CSS override to disable stagger animation. Intentional utility-level override, not abuse. (css/gallery.css:995)

- [ ] [Priority: LOW] [Accessibility] `@media (prefers-reduced-motion: reduce)` CSS block covers photo cards, lightbox transitions, and image transitions, but does not cover the `.calibrate-text` animation or `.scanned-image` color reveal animation. The JS `initScrollReveal()` does check `prefers-reduced-motion`, but the CSS `@keyframes` will still fire for text calibration and scanner effects. Add `animation: none` rules for `.calibrate-text` and `.scanned-image` in the reduced-motion query. (css/gallery.css:1013-1031, 164-188, 200-215)

- [ ] [Priority: LOW] [Code Quality] `<div class="cursor-dot">` and `<div class="cursor-circle">` elements at end of body reference the custom cursor system but no cursor initialization code exists in gallery.js. These elements may be controlled by `main.js` (loaded via defer), but if `main.js` doesn't run on this page, they are dead DOM nodes with no visible purpose. (pages/gallery/index.html:205-206)

## Utilities Home Page

- [ ] [Priority: HIGH] [Accessibility] `outline: none` is set on `:focus-visible` for `.btn-primary-utility` (line 409), `.btn-secondary-utility`/`.demo-chip`/`.control-select`/`.control-input` (line 424), `.btn-overlay-icon` (line 1324), `.stress-mode-button` (line 1592), and `.utilities-buttons button` (line 3007) without a visible focus ring replacement on most selectors. The `box-shadow` on lines 415/427 is subtle and may not meet WCAG 2.4.7 contrast requirements for focus indicators. Add a high-contrast `outline` or `box-shadow` ring to `:focus-visible` states. (css/utilities.css:409, 424, 1324, 1592, 3007)

- [ ] [Priority: HIGH] [Accessibility] The `@media (prefers-reduced-motion: reduce)` block at line 1692 only covers `.stress-mode-button` and `.stress-visual-panel`. It does not cover the title card fade-in animations (`fade-in-home` keyframes in inline `<style>`), the utility shell cross-fade transitions (`is-exiting`/`is-active` class transitions controlled by JS `setTimeout`), or the button stagger entrance animation. Users who prefer reduced motion will still experience all page animations. (css/utilities.css:1692-1697, pages/utilities/index.html:21-81)

- [ ] [Priority: HIGH] [Code Quality / Inconsistency] `UTILITY_MAP` in utilities-shell.js has `'death-calculator'` commented out (line 16), the title card button for Death Calculator is HTML-commented (index.html:107), but the entire death calculator HTML section (~400 lines, lines 339-744) is still rendered in the DOM. This means a massive hidden form is parsed and stored in memory on every page load. Remove the section from HTML or conditionally inject it. (js/utilities-shell.js:16, pages/utilities/index.html:107, 339-744)

- [ ] [Priority: MED] [Accessibility] `.utilities-buttons button:focus-visible` removes outline (line 3007). While a `::before` pseudo-element box-shadow replacement exists (line 3010), the `outline: none` on lines 409 and 424 for `.btn-primary-utility` and `.btn-secondary-utility` etc. rely only on `box-shadow: 0 0 16px -4px rgba(255, 255, 255, 0.1)` as a focus indicator -- this is extremely subtle on dark backgrounds and may not be visible to users with low vision. (css/utilities.css:409, 424)

- [ ] [Priority: MED] [Performance] Inline script at lines 850-867 uses `setTimeout` for staggered button entrance animation. When the page is in a background tab, browsers throttle `setTimeout` which causes delayed buttons to appear all at once when the tab is foregrounded, ruining the stagger effect. Use `requestAnimationFrame` with time-delta tracking or the Web Animations API instead. (pages/utilities/index.html:850-867)

- [ ] [Priority: MED] [Code Quality] `Math.random() - 0.5` sort comparator (line 852) produces a biased shuffle -- it is not a Fisher-Yates shuffle and will not produce a uniform random permutation. For a small array this is cosmetic, but it should use a proper shuffle algorithm for correctness. (pages/utilities/index.html:852)

- [ ] [Priority: MED] [Code Quality] Heavy use of inline styles in the HTML markup -- 12 elements use `style=""` attributes (lines 353, 362, 363, 365, 755, 758, 759, 761, 777, 841, 843, 844). These duplicate and override CSS rules, making maintenance harder. Move to `utilities.css`. (pages/utilities/index.html:353, 362, 363, 365, 755, 758-759, 761, 777, 841-844)

- [ ] [Priority: MED] [Browser Compatibility] `color-mix()` is used extensively in the flair color system (lines 1705-1708, 1714-1718, etc.) without fallback values. Safari < 16.3 and older browsers will fail to parse these declarations, resulting in missing flair colors. Provide fallback properties. (css/utilities.css:1705-1720)

- [ ] [Priority: MED] [Error Handling] `loadLocalAssistantScript()` in utilities-shell.js: if the dynamically loaded script fails (`onerror`), `reject()` is called but the resulting promise rejection is only caught inside `activateStage()` (line 85). If `activateStage` is called outside its expected context or the `.catch` callback itself throws, the rejection is unhandled. Add a top-level `.catch(console.error)` or global unhandled rejection handler. (js/utilities-shell.js:69-73, 85-91)

- [ ] [Priority: MED] [Performance] `window.addEventListener('hashchange', navigateToTarget)` (line 210) is registered but never removed. In a long-lived SPA context or if the module is re-evaluated (e.g., via live-reload during development), duplicate listeners accumulate. Store a reference for cleanup. (js/utilities-shell.js:210)

- [ ] [Priority: LOW] [Code Quality] `backBtn && backBtn.addEventListener(...)` (line 212) uses short-circuit evaluation instead of an `if` statement. Functional but less readable and can confuse linters. Use `if (backBtn) backBtn.addEventListener(...)`. (js/utilities-shell.js:212)

- [ ] [Priority: LOW] [Code Quality] `opts = opts || {}` (line 138) uses the old-style default pattern. Consider `opts = opts ?? {}` to properly handle falsy-but-valid values like `opts = false` or `opts = 0` in future refactors. (js/utilities-shell.js:138)

- [ ] [Priority: LOW] [Code Quality] `pickFlairColor()` uses `Math.floor(Math.random() * FLAIR_COLORS.length)` which is fine, but the result is applied via `stage.style.setProperty('--utility-flair', ...)` (line 81) every time a utility is activated. If the same utility is activated multiple times, it gets a new random color each time -- this may be intentional for "dreamy" effect but creates visual inconsistency on rapid navigation. (js/utilities-shell.js:29-31, 81)

- [ ] [Priority: LOW] [CSS] `.utilities-buttons button:focus-visible { outline: none; }` at line 3007 removes the native focus outline. A `::before` pseudo-element replacement exists on `:is(:hover, :focus-visible)` (line 3010), but the `outline: none` at line 3007 is defined separately without co-locating the replacement, making it easy to accidentally break the focus ring in future edits. (css/utilities.css:3007, 3010)

- [ ] [Priority: LOW] [Code Quality] `<div class="cursor-dot">` and `<div class="cursor-circle">` at end of body (lines 875-876) reference the custom cursor system loaded via `main.js`. If `main.js` fails or doesn't initialize on this page, these elements are dead DOM nodes. (pages/utilities/index.html:875-876)

## Fourier Utility

- [ ] [Priority: HIGH] [Performance] Massive synchronous memory allocation in `buildEnergyBandReconstruction` — `new Float32Array(resolvedBandCount * analysis.samples.length)` can allocate ~336 MB for a 7M sample signal with 12 bands (line 562). This blocks the worker thread for the duration of allocation and GC. Consider chunked allocation or streaming reconstruction. (utilities-src/src/audioFourierCore.ts:562)

- [ ] [Priority: HIGH] [Performance] `buildWindowedFourierAnalysis` creates a plain JS array `Array.from({ length: coefficientCount }, ...)` just to sort indices, then converts to `Uint32Array`. For a detailed preset with ~500K+ coefficients, this creates a large garbage-collection spike. Use a typed-array-friendly sort or an in-place index sort. (utilities-src/src/audioFourierCore.ts:332, 360-368)

- [ ] [Priority: MED] [Error Handling] Empty catch block in `generate()` swallows `audioContext.resume()` failures with no logging (line 549-551). The comment explains the rationale, but in development or debugging mode, logging the error would help identify AudioContext policy violations. (utilities-src/src/audioFourierController.ts:549-551)

- [ ] [Priority: MED] [Error Handling] `decodeAudioData()` catches the original decode error and throws a generic `"Unable to decode this audio file in the browser."` message (lines 606-610). The original error (which might indicate format support, corruption, or memory limits) is completely lost, making debugging decode failures impossible. (utilities-src/src/audioFourierController.ts:606-610)

- [ ] [Priority: MED] [Error Handling] `tryAutoPlayAfterGeneration()` silently catches autoplay failures with no logging (line 938). Autoplay policies vary widely across browsers and contexts (e.g., muted tabs, privacy settings). Logging the rejection in development would help diagnose why autoplay is blocked. (utilities-src/src/audioFourierController.ts:931-947)

- [ ] [Priority: MED] [Performance] `resolveApproximateActiveComponents()` uses `Array.from(this.activeResult.bandGains).join(',')` as a cache key (line 836). Floating-point to-string conversion can produce inconsistent results across JS engines (e.g., `0.1 + 0.2` may stringify differently). Use `toFixed(N)` or integer-encoded keys for reliable caching. (utilities-src/src/audioFourierController.ts:836)

- [ ] [Priority: MED] [Performance] `void processQueue()` in the worker discards the promise (line 266). If an unhandled error escapes `handleAnalyzeRequest` (e.g., a throw in the `finally` block), it becomes an unhandled promise rejection in the worker with no way to report it to the main thread. Wrap with `.catch()` to post an error message. (utilities-src/src/audioFourier.worker.ts:266)

- [ ] [Priority: MED] [Performance] `drawEmptyState()` in `WebGlAudioWaveRenderer` creates a temporary offscreen canvas, draws 2D text, uploads it as a WebGL texture, renders a full-screen quad, and deletes the texture (lines 351-397). If the empty state is redrawn (e.g., after resize or context loss), this generates unnecessary GPU allocations. Cache the texture or use a simpler approach. (utilities-src/src/audioFourierWaveRenderer.ts:351-397)

- [ ] [Priority: MED] [Code Quality] `request as { requestId?: number }` cast in the worker message handler (line 247) bypasses TypeScript's type safety to access `requestId` from an unknown request shape. The `AudioFourierWorkerRequest` union should include `requestId` on all variants, or use a type guard. (utilities-src/src/audioFourier.worker.ts:247)

- [ ] [Priority: MED] [Code Quality] `getAudioFourierPreset()` casts user-supplied string directly to `AudioFourierPresetId` with `id as AudioFourierPresetId` (line 88). If an invalid preset ID bypasses the check, it could cause undefined behavior. The `!preset` guard catches this, but the unsafe cast before the guard is a code smell. (utilities-src/src/audioPresets.ts:88)

- [ ] [Priority: LOW] [Error Handling] `resolveAudioPlaybackButtonLabel` returns `"Play"` for the `isPlaying` state (line 7), which is semantically wrong — it should return `"Pause"` when `isPlaying` is true. The icon is correct (`\u23f8` pause symbol), and the `aria-label` is set to this same label, so screen readers announce "Play" while the button visually shows a pause icon. (utilities-src/src/audioFourierUiState.ts:6-8)

- [ ] [Priority: LOW] [Code Quality] `audioFourierController.test.ts` tests `clamp` and `assertPowerOfTwo` from the math module instead of the controller itself. These utilities should be tested in a dedicated math test file. The actual `AudioFourierController` class has zero test coverage — consider adding E2E or jsdom-based tests for critical paths. (utilities-src/tests/audioFourierController.test.ts:1-56)

- [ ] [Priority: LOW] [Code Quality] `audioFourierWorker.test.ts` only validates type shapes (assignability checks) without testing any runtime behavior. It does not start a worker, send messages, or verify responses. (utilities-src/tests/audioFourierWorker.test.ts:1-49)

- [ ] [Priority: LOW] [Code Quality] `audioFourierWaveRenderer.test.ts` only tests `resolveAudioWaveCanvasScale` (a pure function). The actual `Canvas2dAudioWaveRenderer` and `WebGlAudioWaveRenderer` classes have no test coverage, including their critical rendering and disposal paths. (utilities-src/tests/audioFourierWaveRenderer.test.ts:1-18)

- [ ] [Priority: LOW] [Browser Compatibility] WebGL shaders use `attribute` and `varying` keywords (GLSL ES 1.0 / WebGL 1 syntax). The code requests `webgl2` first, then falls back to `webgl`. In WebGL2 contexts, `attribute`/`varying` are deprecated in favor of `in`/`out` and may produce warnings. Use `#version 300 es` with `in`/`out` for WebGL2 paths, or keep GLSL 1.0 for WebGL1 only. (utilities-src/src/audioFourierWaveRenderer.ts:597-659)

- [ ] [Priority: LOW] [Performance] Scratch arrays (`visualOriginalRawScratch`, `visualMixRawScratch`, `visualOriginalFrameScratch`, `visualMixFrameScratch`) in the controller are lazily grown but never shrink (lines 139-142, 858-867). If a high-resolution analysis loads first, these arrays retain their maximum size even after switching to a smaller analysis. Consider resetting them when `activeResult` changes. (utilities-src/src/audioFourierController.ts:139-142, 858-867)

- [ ] [Priority: LOW] [Error Handling] NaN/Infinity samples propagate through the FFT without sanitization (audioFourierCore.ts lines 244-246 skip NaN in envelope calculations but not in FFT input). The test at line 394-428 confirms NaN propagates to component energies, which could affect energy band partitioning and downstream gain calculations. Consider sanitizing input samples before FFT. (utilities-src/src/audioFourierCore.ts:243-250, 336-340)

## Local Assistant Utility

- [ ] [Priority: HIGH] [Code Quality] Massive code duplication between `js/local-llm-chat.js` and `utilities-src/src/localLlmState.ts`. The entire rendering pipeline is duplicated verbatim: `cleanupModelText`/`cleanupLocalLlmText`, `renderSafeText`/`renderLocalLlmSafeText`, `escapeHtml`, `renderMarkdownInlineText`, `splitInlineLocalLlmSegments`/`splitInlineLocalLlmSegments`, `findClosingInlineMathDelimiter`, `isEscapedAt`, `renderLocalLlmMath`, `LocalLlmLatexParser` class, and `LATEX_COMMANDS` constant (lines 1123-1491 in JS vs lines 17-364 in TS). The JS file should import from the built TS utility module instead of maintaining a parallel copy. Any bug fix in one file must be manually applied to the other. (js/local-llm-chat.js:1123-1491, utilities-src/src/localLlmState.ts:17-364)

- [ ] [Priority: HIGH] [Code Quality] `compactMessagesForWorker` in local-llm-chat.js (lines 1132-1140) duplicates the exact same logic as `compactLocalLlmMessages` in localLlmState.ts (lines 16-36) AND `compactMessages` in local-llm-worker.js (lines 277-293). Three implementations of the same message-compaction logic exist across three files. Consolidate to a single shared function. (js/local-llm-chat.js:1132-1140, utilities-src/src/localLlmState.ts:16-36, js/local-llm-worker.js:277-293)

- [ ] [Priority: MED] [Performance] `renderMessages()` (line 795) rebuilds the entire message list DOM tree on every call — it creates new `article` elements for all messages and replaces `messageList.innerHTML`. During streaming, `updateAssistantElement` efficiently updates only the content div, but `renderMessages` fires on every message completion, history trim, reset, and system notice. Consider a diff-based render or keyed reconciliation to avoid tearing down and rebuilding unchanged messages. (js/local-llm-chat.js:795-820)

- [ ] [Priority: MED] [Performance] `renderStatePanel()` is called from at least 10 different code paths including `updateStatus` (line 489), `updateProgress` (line 367), `updateStatusFromWorker` (line 378), `startLoadingSequence` (line 263), `scheduleLoadingSequenceStep` (line 398), `applyReadyMessage` (line 450), `startPromptCycle` interval every 5s (line 615), `finishAssistantMessage` (line 758), and `renderMessages` (line 819). Each call rebuilds inline styles and DOM attributes. Batch UI updates or use a render queue to reduce redundant paint cycles. (js/local-llm-chat.js:564-602, called from ~10 sites)

- [ ] [Priority: MED] [Error Handling] `dispose()` method (line 931) does not remove the worker's `message` and `error` event listeners. These are stored in `_workerMessageHandler` and `_workerErrorHandler` (set in `ensureWorker` at line 222-233) but `dispose()` only cleans up `_pagehideHandler` and `_deactivateHandler`. If the worker is kept alive after dispose (e.g., via `terminateWorker` with delay), stale listener callbacks could access disposed instance state. Call `terminateWorker` or manually remove worker listeners in `dispose`. (js/local-llm-chat.js:931-951)

- [ ] NOTE: [Priority: MED] [Error Handling] ~~`loadCopy.innerHTML` is set directly on lines 587 and 591~~ — False positive: innerHTML uses `safeCopy` from `renderSafeInlineText(copy)` which internally calls `escapeHtml()`. Matches exemption for innerHTML using escapeHtml/renderSafeText. (js/local-llm-chat.js:587, 591)

- [ ] [Priority: MED] [Code Quality] `normalizeLocalLlmProgressState` in localLlmState.ts (lines 11-14) returns `'loading'` or `'optimizing'`, while `normalizeProgressStatus` in local-llm-worker.js (lines 380-385) returns `'downloading'` or `'loading'`. These functions have similar names but different value domains and are never called from each other — the TS version appears to be a planned refactor that diverges from the worker implementation. Align or consolidate. (utilities-src/src/localLlmState.ts:11-14, js/local-llm-worker.js:380-385)

- [ ] [Priority: MED] [Code Quality] `this._currentCopyTarget` (line 578) and `this._copyTimer` (line 580) are used as instance properties for copy-text fade animation tracking but are never declared in the constructor. They are implicitly created on first access which works but bypasses any potential linting rules and makes the class's state surface area harder to reason about. Declare in constructor as `this._currentCopyTarget = null` and `this._copyTimer = null`. (js/local-llm-chat.js:578, 580)

- [ ] [Priority: MED] [Testing] `localLlmState.test.ts` only covers `compactLocalLlmMessages`, `normalizeLocalLlmProgressState`, and `renderLocalLlmSafeText`. `cleanupLocalLlmText`, `splitInlineLocalLlmSegments`, `renderLocalLlmMath`, and the entire `LocalLlmLatexParser` class have zero test coverage. These are complex parsing functions that handle edge cases. (utilities-src/tests/localLlmState.test.ts)

- [ ] [Priority: MED] [Testing] XSS test at line 46-54 only covers markdown link and image syntax. It does not test raw HTML injection vectors like `<script>`, `<svg onload=...>`, `<img src=x onerror=...>`, or HTML entities embedded in bold/italic rendering. The `renderSafeText` function should be tested against a broader XSS corpus. (utilities-src/tests/localLlmState.test.ts:46-54)

- [ ] [Priority: LOW] [Accessibility] `.local-llm-reset:is(:hover, :focus-visible)` at line 303-309 sets `outline: none` and replaces it with `border-color` and `background` changes. These color changes may not meet WCAG 2.4.7 minimum contrast requirements for focus indicators (2:1 ratio against adjacent colors). Add a visible `box-shadow` or `outline` ring to ensure the focus indicator is perceivable. (css/local-llm-chat.css:303-309)

- [ ] [Priority: LOW] [Accessibility] `resetButton` is disabled via `this.resetButton.disabled = this.isBusy(status)` (line 473) but `aria-disabled` is not explicitly set. While the `disabled` attribute implicitly sets `aria-disabled="true"` in modern browsers, explicitly setting it improves compatibility with older assistive technology. (js/local-llm-chat.js:473)

- [ ] [Priority: LOW] [Browser Compatibility] `color-mix()` is used extensively in local-llm-chat.css (lines 55, 59, 116, 117, 122, 123, 129, 130, 266, 306, 307, 369, 371, 402, 414, 441, 478, 489, 494, 502, 544, 545) without fallback values. Safari < 16.3 and older browsers will silently fail to parse these, resulting in missing or default colors. Provide fallback properties before `color-mix()` declarations. (css/local-llm-chat.css)

- [ ] [Priority: LOW] [Error Handling] `deleteLocalModelCaches()` in local-llm-worker.js (lines 465-476) has an empty catch block that silently returns `false`. Cache API failures (e.g., permissions, quota) are completely invisible. Log the error for debugging. (js/local-llm-worker.js:465-476)

- [ ] [Priority: LOW] [Error Handling] `deleteLocalModelCaches()` in local-llm-chat.js (lines 1480-1491) also has an empty catch block that silently returns `false`. Same issue as the worker version. (js/local-llm-chat.js:1480-1491)

- [ ] [Priority: LOW] [Code Quality] `escapeHtml` in local-llm-chat.js (line 1463-1471) leaves backticks literal for inline-code parsing, while `escapeHtml` in localLlmState.ts (line 357-364) escapes backticks to `&#39;` (actually it escapes single quotes, not backticks — but the functions have different escape sets). The JS version escapes `&`, `<`, `>`, `"`, `'` while the TS version does the same. The comment in JS says "backticks are intentionally left literal" but backticks aren't in either escape set. The functions are functionally identical. Consolidate to avoid drift. (js/local-llm-chat.js:1463-1471, utilities-src/src/localLlmState.ts:357-364)

## Virtual Machine Utility

- [ ] NOTE: [Priority: HIGH] [Performance] ~~`reset()` calls `this.screenContainer.innerHTML = ''` (line 608)~~ — False positive: innerHTML that clears to '' is not a security concern. The performance observation (canvas teardown triggering reflow) is valid but `innerHTML = ''` is the idiomatic and fastest approach in modern browsers, and this only runs on user-initiated reset. (utilities-src/src/retroVmController.ts:608)

- [ ] [Priority: HIGH] [Error Handling] `beforeUnloadHandler` is registered on `pagehide` (line 439) but calls `void this.destroySession()` which discards the promise. If `destroySession()` throws or takes a long time, the unhandled rejection is lost. The `pagehide` event gives only ~5 seconds before the page is frozen — consider using `beforeunload` with `event.preventDefault()` instead of `pagehide`, or use a synchronous cleanup path. (utilities-src/src/retroVmController.ts:330-332, 439)

- [ ] [Priority: MED] [Security] `navigator.clipboard.readText()` in `pasteClipboard()` (line 573) reads the system clipboard which may contain sensitive data. The clipboard contents are then sent directly to the VM via `keyboard_send_text()` (line 580) without any sanitization or warning beyond a truncation notice. A user could accidentally paste passwords, tokens, or other secrets into the guest OS. Consider adding a confirmation dialog for clipboard paste. (utilities-src/src/retroVmController.ts:567-590)

- [ ] [Priority: MED] [Performance] `queueGuestFitSync()` uses `requestAnimationFrame` (line 722) but does NOT guard against multiple calls per frame — `if (this.resizeFrameId) { return; }` only prevents scheduling a second frame while one is already pending, which is correct. However `resizeHandler` (line 349-351) fires on every `window.resize` event without RAF throttling and calls `queueGuestFitSync()` directly. The `ResizeObserver` callback (line 448-450) also calls `queueGuestFitSync()`. These three sources (window resize, resize observer, fullscreen change) can all compete. The RAF guard prevents per-pixel reflows but three separate RAF slots can still fire in the same frame if sources fire rapidly. (utilities-src/src/retroVmController.ts:349-351, 448-450, 717-726)

- [ ] [Priority: MED] [TypeScript] `value as V86DownloadProgress` cast in `onDownloadProgress` (line 369) is an unsafe assertion — `value` could be any shape from the v86 bus. If v86 changes its event payload shape, the property access on `next.loaded` etc. would silently return `undefined`. Use a runtime type guard or `Array.isArray` / property existence checks before the cast. (utilities-src/src/retroVmController.ts:369)

- [ ] [Priority: MED] [TypeScript] `new V86(...) as V86` cast at line 515 — the v86 library's constructor returns `any` or a broad type. The cast hides type mismatches and the comment admits "v86's constructor type is broader than the runtime emulator surface used here." This is the correct approach given v86's typing, but document it as an explicit `@ts-expect-error` instead of a cast so that future type changes in v86 don't silently bypass type checking. (utilities-src/src/retroVmController.ts:515)

- [ ] [Priority: MED] [Error Handling] Empty catch blocks in `requestPointerLock()` (lines 271-276) and `exitFullscreenIfNeeded()` (line 780-782) silently suppress pointer-lock and fullscreen errors. While the comments explain the rationale, these suppressions are invisible in production. Consider adding a `console.debug()` or conditional logging for debugging degraded browser experiences. (utilities-src/src/retroVmController.ts:271-276, 780-782)

- [ ] [Priority: MED] [Error Handling] `autoAdvanceBootMenu()` has an empty catch block (line 649-656) that swallows `wait_until_vga_screen_contains` errors. The fallback behavior (dispatching Enter after a delay) is reasonable, but the error is completely invisible. Add conditional debug logging. (utilities-src/src/retroVmController.ts:649-656)

- [ ] [Priority: MED] [Error Handling] `resolveRetroVmConfigFromDataset` at line 400 uses `root.dataset as unknown as RetroVmDatasetConfig` — an unsafe double cast. If the dataset contains unexpected properties, they silently pass through. More concerning: if HTML data attributes change without a corresponding TS interface update, the cast hides the mismatch. Use a field-by-field extraction instead. (utilities-src/src/retroVmController.ts:399)

- [ ] [Priority: MED] [Browser Compatibility] `requestPointerLock({ unadjustedMovement: true })` at line 270 uses the `unadjustedMovement` option which is not supported in Firefox (as of 131). The fallback to `requestPointerLock()` without options on catch (line 272) handles this, but the nested try/catch structure makes it harder to read. (utilities-src/src/retroVmController.ts:269-273)

- [ ] [Priority: MED] [Browser Compatibility] `navigator.clipboard.readText()` requires HTTPS and user gesture context. The `pasteClipboard()` method (line 567-590) is triggered by a button click so gesture context is satisfied, but it will fail in HTTP contexts. The error message says "Clipboard access was denied" which is accurate but doesn't mention the HTTPS requirement. (utilities-src/src/retroVmController.ts:567-590)

- [ ] [Priority: MED] [Code Quality] `RetroVmMouseBridge` class stores bound arrow functions as class properties (lines 98-128). This is a valid pattern but each instance creates 8 separate closure objects. For a singleton pattern (only one VM controller per page), this is fine, but if multiple VMs are supported in the future, consider binding in the constructor to reduce per-instance object allocations. (utilities-src/src/retroVmController.ts:98-128)

- [ ] [Priority: LOW] [Code Quality] `formatBytes()` function (retroVmSupport.ts:91-100) returns KB-rounded values without decimal places for the KB range (1-1024 KB shows as integer KB) but uses `.toFixed(1)` for MB. For a 20 MB ISO showing ~15360 KB during early download, the jump from KB to MB format can look like progress jumped backwards due to rounding. Consider using a consistent decimal precision. (utilities-src/src/retroVmSupport.ts:99)

- [ ] [Priority: LOW] [Code Quality] `FAKE_ISO_SIZE_BYTES = 20_082_688` constant at line 11 matches the actual ISO size exactly. If the ISO is rebuilt with a different size, the fake progress in test mode will report a different byte count than the real download, making test assertions fragile. Consider deriving this from `RETRO_VM_CONFIG.cdromSizeBytes`. (utilities-src/src/retroVmController.ts:11)

- [ ] [Priority: LOW] [Code Quality] `MAX_CLIPBOARD_PASTE_CHARS = 2048` (line 12) is a hardcoded limit that could be moved to `RetroVmConfig` for configurability, especially since the truncation message (line 582-583) embeds the exact limit in the user-facing string. (utilities-src/src/retroVmController.ts:12, 582-583)

- [ ] [Priority: LOW] [Code Quality] `resolveRetroVmConfigFromDataset` in retroVmConfig.ts: lines 73-83 manually spread and override each copy field. If `RetroVmCopyConfig` gains a new field, this function must be updated or the dataset override silently falls back to default. Consider automating this with `Object.fromEntries` or a typed spread utility. (utilities-src/src/retroVmConfig.ts:73-83)

- [ ] [Priority: LOW] [Testing] No test coverage for `RetroVmController` itself — the test files only cover `retroVmSupport` (support helpers, state machine, formatting) and `retroVmConfig` (config resolution, v86 options). The controller, which contains the core logic for lifecycle management, clipboard paste, fullscreen, mouse capture, and error recovery, has zero test coverage. Consider adding jsdom-based unit tests or integration tests for critical paths. (utilities-src/tests/)

- [ ] [Priority: LOW] [Testing] `retroVmConfig.test.ts` casts `{}` as `HTMLElement` at line 31 for `screenContainer`. This bypasses DOM type checking and means `buildRetroVmV86Options` is never tested against an actual element. If the function accesses element properties (e.g., `screen_container.id`), this would fail at runtime but pass tests. (utilities-src/tests/retroVmConfig.test.ts:31)

- [ ] [Priority: LOW] [Code Quality] `readMatchMedia` function in retroVmSupport.ts wraps `matchMedia()` in try/catch (lines 3-13) which is good defensive coding, but the catch swallows all errors including potential security policy violations. Consider logging at debug level in non-production environments. (utilities-src/src/retroVmSupport.ts:8-12)

- [ ] [Priority: LOW] [Code Quality] `BOOT_MENU_SECOND_ENTER_DELAY_MS = 900` (line 13) uses a fixed 900ms delay for the second Enter key dispatch. This magic number has no explanation. If the boot menu timing changes (e.g., different distro), this needs adjustment. Add a comment explaining why 900ms was chosen. (utilities-src/src/retroVmController.ts:13)

- [ ] [Priority: LOW] [Shell Script] `build-retro-vm-image.sh` copies `flwm_topside.tcz` and its md5 file (lines 72-73) without verifying the md5 checksum. If the `.tcz` file is corrupted or modified, the ISO will be built with an invalid extension. Add an md5 verification step before copying. (scripts/build-retro-vm-image.sh:72-73)

- [ ] [Priority: LOW] [Shell Script] `build-retro-vm-image.sh` uses `alpine:3.21` which pins a specific Alpine version. When Alpine 3.21 reaches EOL, the build will fail if the image is removed from Docker Hub. Consider using `alpine:latest` or updating the pinned version when 3.21 is deprecated. (scripts/build-retro-vm-image.sh:49)

## Stress Test Utility

- [ ] [Priority: HIGH] [Performance] `new Float32Array([now * 0.001, this.canvas.width, this.canvas.height, 0])` is allocated inside every WebGPU animation frame (line 710). During stress testing at 60fps, this generates ~60 small GC allocations per second. Reuse a single `Float32Array` instance declared outside the frame loop and mutate its values each frame. (utilities-src/src/stressTestController.ts:710)

- [ ] [Priority: MED] [Performance] `gl.getAttribLocation(program, 'a_position')` is called on every WebGL frame (line 849). This function walks the active attribute list and is relatively expensive. Cache the result during shader compilation instead of querying every frame. (utilities-src/src/stressTestController.ts:849)

- [ ] [Priority: MED] [Performance] `resizeCanvas()` is called on every animation frame (lines 700, 844) which invokes `getBoundingClientRect()`, forcing synchronous layout recalculation. While the width/height check prevents unnecessary reassignment, the forced layout still happens. Consider calling this only on `resize` events, not per-frame. (utilities-src/src/stressTestController.ts:700, 844, 1210-1217)

- [ ] [Priority: MED] [Performance] `syncControlPanelFit()` reads `controlPanel.scrollHeight` and `controlPanel.clientHeight` inside a `for` loop (line 1195). Each read forces layout recalculation, and writing `card.hidden = true` in the same loop (line 1200) triggers additional reflows. Batch the reads before writes, or defer the visibility changes. (utilities-src/src/stressTestController.ts:1193-1207)

- [ ] [Priority: MED] [Error Handling] `device.lost.then(...)` has no `.catch()` (line 679-693). If any code in the `then` callback throws (e.g., `setState` or `syncMetrics` throws), the error becomes an unhandled promise rejection with no way to surface it. Add a `.catch(console.error)` at minimum. (utilities-src/src/stressTestController.ts:679)

- [ ] [Priority: MED] [TypeScript] `ActiveWebGpuStress` interface uses `unknown` for `computePipeline`, `renderPipeline`, and `renderBindGroup` (lines 83-86). These could be strongly typed as `GPUComputePipeline`, `GPURenderPipeline`, and `GPUBindGroup` respectively for better type safety and autocomplete. (utilities-src/src/stressTestController.ts:83-86)

- [ ] [Priority: MED] [TypeScript] `canvas.getContext('webgpu') as unknown as WebGpuCanvasContextLike | null` (line 534) is a double `as unknown as` cast that completely bypasses type checking. The native `GPUCanvasContext` type could be used instead of the custom interface. (utilities-src/src/stressTestController.ts:534)

- [ ] [Priority: MED] [TypeScript] `element as T` in `requireElement` (line 1270) is an unsafe cast. `getElementById` returns `HTMLElement | null`, but `T` could be a more specific type. If the HTML structure changes so the element is no longer the expected type, the cast hides this at compile time. (utilities-src/src/stressTestController.ts:1270)

- [ ] [Priority: LOW] [Error Handling] Empty catch block `catch { return false; }` in `canCreateContext` (line 501-503) silently swallows all errors during WebGL context probing. While intentional defensive coding, a `console.debug()` in non-production builds would help diagnose context creation failures. (utilities-src/src/stressTestController.ts:501-503)

- [ ] [Priority: LOW] [Code Quality] `stopCpuStress` accepts `_requestId` parameter (line 421) that is never used. The underscore prefix indicates awareness, but the parameter adds noise. Remove it unless there's a plan to use it for request-scoped stopping. (utilities-src/src/stressTestController.ts:421)

- [ ] [Priority: LOW] [Code Quality] The built JS file `pages/utilities/assets/stressTestController.js` is a single-line minified output (~25KB) with no source map reference. Debugging production issues in this file is nearly impossible. Ensure the Vite build outputs source maps (`build.sourcemap: true`) at least for staging deployments. (pages/utilities/assets/stressTestController.js)

- [ ] [Priority: LOW] [Performance] `resolveWebGlWorkloadLevel` (line 877-885) calls `performance.now()` and does arithmetic on every frame to adjust workload. The workload only changes every 750ms (`Math.floor(elapsed / 750)` ramp). Cache the ramp level and only recompute every 750ms to reduce per-frame work. (utilities-src/src/stressTestController.ts:877-885)

- [ ] [Priority: LOW] [Browser Compatibility] WebGL fallback shaders use GLSL ES 1.0 `attribute`/`varying` keywords (line 751) which are deprecated in WebGL2 (`#version 300 es` with `in`/`out` is preferred). This works but may produce deprecation warnings in WebGL2 contexts. (utilities-src/src/stressTestController.ts:751-754)

- [ ] [Priority: LOW] [Accessibility] The `<canvas id="stressCanvas">` element's `aria-label` is only set during `replaceCanvasElement()` (line 1228) with a fallback of `'Stress test output'`. The initial canvas in the HTML may lack an `aria-label` if not set in the markup. Ensure the initial HTML canvas has `aria-label="Stress test visualization"` so screen readers can identify it before any JS runs. (utilities-src/src/stressTestController.ts:1228)
